# Codex 产出审核 Checklist

每次 Codex 提交代码后，逐项检查：

## 基础检查

- [ ] **1. git status** — 是否只有本轮任务的变更文件
- [ ] **2. git diff** — 是否有预期之外的改动
- [ ] **3. 是否引入超范围功能** — 代码里是否有 Phase 2+ 的功能（推荐、AI、爬虫等）
- [ ] **4. 是否有硬编码路径** — 如 `/home/user/`、`C:\Users\` 等
- [ ] **5. 是否有敏感信息泄露** — API Key、密码、路径泄露
- [ ] **6. 是否有测试** — 对应功能的 `tests/` 文件
- [ ] **7. 类型注解** — 所有函数有类型提示

## 功能检查

- [ ] **8. API 是否符合路由设计** — 对照 PLAN.md 的 API 路由表
- [ ] **9. 数据库迁移是否安全** — 新增表/列不会破坏已有数据
- [ ] **10. 前端页面能否在局域网访问** — 不是 `localhost` 绑定
- [ ] **11. 错误处理** — 输入校验、404 处理、异常捕获
- [ ] **12. 日志是否脱敏** — 没有打印 `extra`、`summary` 等字段的原始值

## 部署检查

- [ ] **13. `docker compose build` 是否通过**
- [ ] **14. `docker compose up` 能否正常启动**
- [ ] **15. `/api/` 端点是否能 curl 通**
- [ ] **16. SQLite 文件是否生成在 `data/` 目录下**
- [ ] **17. Docker 卷挂载是否正确**

## 常见违规

```
❌ 引入外部爬虫代码（Phase 1 不做）
❌ 写入 token/cookie 管理逻辑
❌ 引入推荐/ML 依赖
❌ 修改 database.py 加入不需要的表
❌ 引入 JS 前端框架（用 HTMX 不是 React/Vue）
❌ 提交了 .env 文件（应加 .gitignore）
```

## 验收通过标准

全部 [✅] → 通知你确认
≥1 个 [❌] → 退回 Codex 修复，说明哪个检查项不通过
