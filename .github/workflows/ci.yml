name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install development dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Check installed dependency consistency
        run: python -m pip check

      - name: Run tests
        run: python -m pytest

  docker-smoke:
    name: Docker production smoke
    runs-on: ubuntu-latest
    # Fixed job-level paths so always() cleanup can locate resources even if the
    # prepare step fails before creating files.
    env:
      SMOKE_DIR: /tmp/nsfwtrack-docker-smoke
      COMPOSE_PROJECT_NAME: nsfwtrack-ci-smoke
      COMPOSE_FILE: /tmp/nsfwtrack-docker-smoke/docker-compose.yml

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Prepare isolated CI data and temporary credentials
        run: |
          set -euo pipefail
          # The image runs as fixed UID/GID 10001:10001 and has no capability
          # available to bypass host bind-mount permissions.
          mkdir -p "${SMOKE_DIR}"
          sudo install -d -m 0700 -o 10001 -g 10001 "${SMOKE_DIR}/data"
          APP_PASSWORD="$(python3 -c 'import secrets; print(secrets.token_urlsafe(24))')"
          SECRET_KEY="$(python3 -c 'import secrets; print(secrets.token_hex(32))')"
          # Temporary CI-only credentials. Never use .env.example placeholders.
          {
            echo "APP_PASSWORD=${APP_PASSWORD}"
            echo "SECRET_KEY=${SECRET_KEY}"
            echo "DATABASE_URL=sqlite:///data/nsfwtrack.db"
          } > "${SMOKE_DIR}/.env"
          {
            echo "services:"
            echo "  nsfwtrack:"
            echo "    build: ${GITHUB_WORKSPACE}"
            echo "    env_file:"
            echo "      - ${SMOKE_DIR}/.env"
            echo "    read_only: true"
            echo "    cap_drop:"
            echo "      - ALL"
            echo "    security_opt:"
            echo "      - no-new-privileges:true"
            echo "    tmpfs:"
            echo "      - /tmp:size=64m,mode=1777"
            echo "    ports:"
            echo '      - "18080:8000"'
            echo "    volumes:"
            echo "      - ${SMOKE_DIR}/data:/app/data"
            echo '    restart: "no"'
          } > "${COMPOSE_FILE}"

      - name: Build production image
        run: docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" build

      - name: Start container
        run: docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" up -d

      - name: Wait for container health
        run: |
          set -euo pipefail
          container_id="$(docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" ps -q nsfwtrack)"
          if [ -z "${container_id}" ]; then
            echo "docker smoke failed: container was not created" >&2
            exit 1
          fi
          deadline=$((SECONDS + 90))
          while [ "${SECONDS}" -lt "${deadline}" ]; do
            health="$(docker inspect --format='{{if .State.Health}}{{.State.Health.Status}}{{else}}missing{{end}}' "${container_id}")"
            echo "container health: ${health}"
            if [ "${health}" = "healthy" ]; then
              exit 0
            fi
            running="$(docker inspect --format='{{.State.Running}}' "${container_id}")"
            if [ "${running}" != "true" ]; then
              echo "docker smoke failed: container stopped before becoming healthy" >&2
              exit 1
            fi
            sleep 2
          done
          echo "docker smoke failed: container did not become healthy" >&2
          exit 1

      - name: Verify runtime security and writable paths
        run: |
          set -euo pipefail
          container_id="$(docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" ps -q nsfwtrack)"
          test -n "${container_id}"

          test "$(docker inspect --format='{{.Config.User}}' "${container_id}")" = "10001:10001"
          test "$(docker inspect --format='{{.HostConfig.ReadonlyRootfs}}' "${container_id}")" = "true"
          docker inspect --format='{{json .HostConfig.CapDrop}}' "${container_id}" | grep -q '"ALL"'
          docker inspect --format='{{json .HostConfig.SecurityOpt}}' "${container_id}" | grep -q '"no-new-privileges:true"'
          test "$(docker inspect --format='{{if index .HostConfig.Tmpfs "/tmp"}}configured{{end}}' "${container_id}")" = "configured"

          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" exec -T nsfwtrack sh -eu -c '
            data_probe=/app/data/.ci-write-probe
            tmp_probe=/tmp/.ci-write-probe
            trap "rm -f ${data_probe} ${tmp_probe}" EXIT
            test "$(id -u)" = "10001"
            test "$(id -g)" = "10001"
            test "$(awk "/^CapEff:/ {print \$2}" /proc/1/status)" = "0000000000000000"
            test "$(awk "/^NoNewPrivs:/ {print \$2}" /proc/1/status)" = "1"
            : > "${data_probe}"
            : > "${tmp_probe}"
            mkdir -p /app/data/media
            python -c "from pathlib import Path; Path(\"/app/data/media/ci.gif\").write_bytes(b\"GIF89a\\x01\\x00\\x01\\x00;\")"
            python -c "from app.database import SessionLocal; from app.services.media_index import refresh_media_index; db = SessionLocal(); result = refresh_media_index(db, full=True); assert result.status.entry_count == 1; assert result.status.rehashed_count == 1; db.close()"
            python -c "import os, stat; from pathlib import Path; from app.services.media_operation_lock import media_operation_lock, media_operation_lock_path; context = media_operation_lock(); handle = context.__enter__(); handle.verify(); context.__exit__(None, None, None); path = media_operation_lock_path(); info = os.stat(path, follow_symlinks=False); assert path.parent == Path(\"/app/data\"); assert not path.is_relative_to(Path(\"/app/data/media\")); assert not path.is_symlink(); assert stat.S_ISREG(info.st_mode); assert stat.S_IMODE(info.st_mode) == 0o600; assert info.st_uid == os.geteuid() == 10001"
            for target in /app/.ci-readonly-probe /etc/.ci-readonly-probe /usr/local/.ci-readonly-probe; do
              if touch "${target}" 2>/dev/null; then
                echo "unexpected writable path: ${target}" >&2
                rm -f "${target}" || true
                exit 1
              fi
            done
            test -s /app/data/nsfwtrack.db
            python -c "import sqlite3; connection = sqlite3.connect(\"/app/data/nsfwtrack.db\"); assert connection.execute(\"SELECT MAX(version) FROM schema_migrations\").fetchone()[0] == 5; assert connection.execute(\"SELECT valid FROM media_index_state WHERE id = 1\").fetchone()[0] == 1; assert connection.execute(\"SELECT COUNT(*) FROM media_index_entries WHERE record_type = char(109,101,100,105,97)\").fetchone()[0] == 1"
          '
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" exec -T nsfwtrack python - <<'PY'
          import http.cookiejar
          import json
          import os
          import pathlib
          import urllib.request

          assert not pathlib.Path("/app/tests").exists()
          jar = http.cookiejar.CookieJar()
          opener = urllib.request.build_opener(
              urllib.request.HTTPCookieProcessor(jar)
          )
          login = urllib.request.Request(
              "http://127.0.0.1:8000/api/auth/login",
              data=json.dumps({"password": os.environ["APP_PASSWORD"]}).encode(),
              headers={"Content-Type": "application/json"},
              method="POST",
          )
          with opener.open(login, timeout=5) as response:
              assert response.status == 200
          with opener.open("http://127.0.0.1:8000/source-search", timeout=5) as response:
              body = response.read().decode("utf-8")
              assert response.status == 200
              assert "Traceback" not in body
              assert 'action="/source-search/search"' not in body
          PY

      - name: Recreate container and verify SQLite persistence
        run: |
          set -euo pipefail
          sudo test -s "${SMOKE_DIR}/data/nsfwtrack.db"
          sudo test -f "${SMOKE_DIR}/data/.nsfwtrack-media-operation.lock"
          sudo test ! -L "${SMOKE_DIR}/data/.nsfwtrack-media-operation.lock"
          test "$(sudo stat -c '%a:%u:%g' "${SMOKE_DIR}/data/.nsfwtrack-media-operation.lock")" = "600:10001:10001"
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" stop nsfwtrack
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" rm -f nsfwtrack
          sudo test -s "${SMOKE_DIR}/data/nsfwtrack.db"
          sudo test -f "${SMOKE_DIR}/data/.nsfwtrack-media-operation.lock"
          test "$(sudo stat -c '%a:%u:%g' "${SMOKE_DIR}/data/.nsfwtrack-media-operation.lock")" = "600:10001:10001"
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" up -d nsfwtrack

          container_id="$(docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" ps -q nsfwtrack)"
          test -n "${container_id}"
          deadline=$((SECONDS + 90))
          while [ "${SECONDS}" -lt "${deadline}" ]; do
            health="$(docker inspect --format='{{if .State.Health}}{{.State.Health.Status}}{{else}}missing{{end}}' "${container_id}")"
            echo "recreated container health: ${health}"
            if [ "${health}" = "healthy" ]; then
              break
            fi
            running="$(docker inspect --format='{{.State.Running}}' "${container_id}")"
            if [ "${running}" != "true" ]; then
              echo "docker smoke failed: recreated container stopped before becoming healthy" >&2
              exit 1
            fi
            sleep 2
          done
          test "${health}" = "healthy"
          test "$(docker inspect --format='{{.Config.User}}' "${container_id}")" = "10001:10001"
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" exec -T nsfwtrack sh -eu -c '
            test "$(id -u)" = "10001"
            test "$(id -g)" = "10001"
            test -s /app/data/nsfwtrack.db
            python -c "import os, stat; from app.services.media_operation_lock import media_operation_lock, media_operation_lock_path; context = media_operation_lock(); handle = context.__enter__(); handle.verify(); context.__exit__(None, None, None); info = os.stat(media_operation_lock_path(), follow_symlinks=False); assert stat.S_ISREG(info.st_mode); assert stat.S_IMODE(info.st_mode) == 0o600; assert info.st_uid == os.geteuid() == 10001"
            python -c "from pathlib import Path; from app.database import SessionLocal; from app.services.media_index import get_media_index_status; from app.services.media_write_coordination import MediaFilesystemOutcome, MediaIndexCoordinationStatus, coordinate_media_mutation; db = SessionLocal(); coordinated = coordinate_media_mutation(db, source=\"post_upload\", operation=lambda: Path(\"/app/data/media/recreated.gif\").write_bytes(b\"GIF89a\\x01\\x00\\x01\\x00x;\"), classify_result=lambda _result: MediaFilesystemOutcome.FILESYSTEM_CHANGED_KNOWN, classify_error=lambda _error: MediaFilesystemOutcome.FILESYSTEM_OUTCOME_UNKNOWN); status = get_media_index_status(db); assert coordinated.index.status == MediaIndexCoordinationStatus.SYNCHRONIZED; assert status.usable; assert status.entry_count == 2; assert status.last_refresh_source == \"post_upload\"; db.close()"
            python -c "import sqlite3; connection = sqlite3.connect(\"/app/data/nsfwtrack.db\"); assert connection.execute(\"SELECT MAX(version) FROM schema_migrations\").fetchone()[0] == 5; assert connection.execute(\"SELECT valid FROM media_index_state WHERE id = 1\").fetchone()[0] == 1; assert connection.execute(\"SELECT COUNT(*) FROM media_index_entries WHERE record_type = char(109,101,100,105,97)\").fetchone()[0] == 2"
          '

      - name: Wait for /login and verify security headers
        run: |
          set -euo pipefail
          python3 <<'PY'
          import sys
          import time
          import urllib.request

          url = "http://127.0.0.1:18080/login"
          required = {
              "x-content-type-options": "nosniff",
              "referrer-policy": "strict-origin-when-cross-origin",
              "x-frame-options": "DENY",
          }
          deadline = time.time() + 60
          last_error = "no response"
          while time.time() < deadline:
              try:
                  with urllib.request.urlopen(url, timeout=5) as response:
                      status = response.getcode()
                      headers = {k.lower(): v for k, v in response.headers.items()}
                      if status != 200:
                          last_error = f"status={status}"
                      else:
                          missing = [
                              f"{name} expected {expected!r} got {headers.get(name)!r}"
                              for name, expected in required.items()
                              if headers.get(name) != expected
                          ]
                          permissions = headers.get("permissions-policy", "")
                          if "camera=()" not in permissions:
                              missing.append(
                                  f"permissions-policy missing camera=(): {permissions!r}"
                              )
                          request_id = headers.get("x-request-id", "")
                          if not request_id:
                              missing.append("missing x-request-id")
                          if "strict-transport-security" in headers:
                              missing.append("unexpected HSTS")
                          if "content-security-policy" in headers:
                              missing.append("unexpected CSP")
                          if not missing:
                              print("docker smoke ok")
                              print(f"status={status}")
                              for name in list(required) + [
                                  "permissions-policy",
                                  "x-request-id",
                              ]:
                                  print(f"{name}: {headers.get(name)}")
                              sys.exit(0)
                          last_error = "; ".join(missing)
              except Exception as exc:  # noqa: BLE001 - smoke wait loop
                  last_error = f"{type(exc).__name__}: {exc}"
              time.sleep(2)
          print(f"docker smoke failed: {last_error}", file=sys.stderr)
          sys.exit(1)
          PY

      - name: Show container logs on failure
        if: failure()
        run: |
          set +e
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" ps -a || true
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" logs --no-color || true

      - name: Clean up containers and temporary data
        if: always()
        run: |
          set +e
          docker compose -f "${COMPOSE_FILE}" -p "${COMPOSE_PROJECT_NAME}" down --rmi local --volumes --remove-orphans || true
          if [ -d "${SMOKE_DIR}" ]; then
            sudo rm -rf "${SMOKE_DIR}"
          fi
          echo "docker smoke cleanup complete"
          echo "cleanup targets: SMOKE_DIR=${SMOKE_DIR} COMPOSE_FILE=${COMPOSE_FILE} COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME}"
