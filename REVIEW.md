# 开发产出审核 Checklist

每次开发提交代码后，逐项检查：

## 当前网络边界检测（一票否决）

- [ ] **0. Phase 3-A1 是否仅保存用户提供 URL、解析本地书签 HTML / 纯文本清单，且没有外部 HTTP 请求、爬虫、adapter、远程图片、自动同步、推荐或 AI？**

> 保存 URL 不等于请求 URL。出现任何网络获取或自动采集行为 → 直接判定为**超范围**，退回开发修复。

## 基础检查

- [ ] **1. git status** — 是否只有本轮任务的变更文件
- [ ] **2. git diff** — 是否有预期之外的改动
- [ ] **3. 是否引入超范围功能** — 代码里是否有 Phase 2+ 的功能（推荐、AI、爬虫、外部源等）
- [ ] **4. 是否有硬编码路径** — 如 `/home/user/`、`C:\Users\` 等
- [ ] **5. 是否有敏感信息泄露** — API Key、密码、路径泄露
- [ ] **6. 是否有测试** — 对应功能的 `tests/` 文件
- [ ] **7. 类型注解** — 所有函数有类型提示
- [ ] **7.1. i18n 覆盖** — Web UI 中文 / English 文案是否完整，默认是否为中文
- [ ] **7.2. 页面提示体验** — 成功、错误、普通信息提示是否统一、可读且不泄露敏感信息

## 功能检查

- [ ] **8. API 是否符合路由设计** — 对照 PLAN.md 的 API 路由表
- [ ] **9. 数据库迁移是否安全** — 新增表/列不会破坏已有数据
- [ ] **10. 前端页面能否在局域网访问** — 不是 `localhost` 绑定
- [ ] **11. 错误处理** — 输入校验、404 处理、异常捕获
- [ ] **12. 日志是否脱敏** — 没有打印密码、cookie、token
- [ ] **12.1. 备份恢复是否安全** — 是否要求登录、只处理本地上传文件、不从 URL 恢复、失败时不破坏现有数据库

## Phase 2-A1 列表页增强检查

- [ ] **12.2. 高级筛选是否本地化** — 关键词、状态、单标签、单创作者、最低评分和时间范围是否只查询本地 SQLite 数据
- [ ] **12.3. 筛选参数是否稳定** — query string 是否可刷新、可复制，非法 `page` / `page_size` 是否安全回退且不触发 500
- [ ] **12.4. 排序是否完整** — 是否覆盖创建时间、更新时间、标题和评分的正反向排序，默认是否为最近更新
- [ ] **12.5. 分页体验是否完整** — 是否支持 `10` / `20` / `50` / `100` 分页大小，页码跳转是否保留筛选和排序条件
- [ ] **12.6. 列表页 UI 状态是否清晰** — 是否保留表单输入，展示当前筛选条件，提供清空筛选和无匹配结果提示
- [ ] **12.7. 列表页 i18n 是否完整** — 新增筛选、排序、分页和空状态文案是否同时覆盖中文 / English
- [ ] **12.8. Phase 2-A1 边界是否保持** — 是否未引入外部内容源、爬虫、adapter、远程图片拉取、自动同步、多源搜索、推荐系统或 AI 助手

## Phase 2-A2 批量编辑检查

- [ ] **12.9. 批量选择是否限于当前页** — 是否支持单项选择、全选当前页和取消选择，且未实现跨页全选
- [ ] **12.10. 批量操作是否要求登录** — 未登录执行批量操作是否跳转登录或被拒绝
- [ ] **12.11. 批量状态 / 标签 / 评分是否安全** — 非法状态、评分、缺失标签和不存在标签是否安全拒绝且不触发 500
- [ ] **12.12. 批量删除是否安全** — 是否有二次确认、危险提示，且只删除选中条目并清理本地关联数据
- [ ] **12.13. 批量操作结果是否清晰** — 成功 / 失败 flash 是否展示处理数、跳过数和明确原因
- [ ] **12.14. 批量操作后是否保留列表状态** — 是否通过安全站内 `next` 返回原筛选、排序、分页和 page_size
- [ ] **12.15. 批量编辑 i18n 是否完整** — 新增多选、批量状态、标签、评分、删除和错误提示是否同时覆盖中文 / English
- [ ] **12.16. Phase 2-A2 边界是否保持** — 是否未引入外部内容源、爬虫、adapter、远程图片拉取、自动同步、多源搜索、推荐系统、AI 助手或大型前端框架

## Phase 2-A3 详情页增强检查

- [ ] **12.17. 详情页分区是否清晰** — 是否包含基本信息、状态信息、标签信息、创作者信息和操作区域
- [ ] **12.18. 详情页状态编辑是否安全** — 状态 / 评分 / 短评保存是否要求登录，非法状态和评分是否安全拒绝且不触发 500
- [ ] **12.19. 详情页标签管理是否受限本地已有标签** — 是否只允许添加已有标签，不自动创建新标签，重复关联不会重复写入
- [ ] **12.20. 详情页创作者管理是否受限本地已有创作者** — 是否只允许关联已有创作者，不自动创建新创作者，重复关联不会重复写入
- [ ] **12.21. 详情页返回列表上下文是否安全** — `next` 是否只接受站内相对路径，并保留搜索、筛选、排序、page 和 page_size
- [ ] **12.22. 详情页删除是否仍有确认** — 删除条目按钮是否保持危险样式并使用二次确认
- [ ] **12.23. 详情页 i18n 是否完整** — 新增详情分区、状态、标签、创作者、返回列表和错误提示是否同时覆盖中文 / English
- [ ] **12.24. Phase 2-A3 边界是否保持** — 是否未引入外部内容源、爬虫、adapter、远程图片拉取、自动同步、多源搜索、推荐系统、AI 助手、云同步、多用户系统或大型前端框架

## Phase 2-A4 导入增强检查

- [ ] **12.25. 导入模板是否受登录保护** — CSV / JSON 模板下载是否要求登录，文件名和示例字段是否符合本地导入说明
- [ ] **12.26. 导入字段说明是否清晰** — 页面是否说明 CSV / JSON 支持字段、`title` 必填、内部 `status` 值、`rating` 1-5、`tags` / `creators` 当前导入逻辑和本地上传边界
- [ ] **12.27. CSV 字段映射是否安全** — 是否读取源表头，支持自动匹配和手动映射，缺少 `title`、重复映射和无效映射是否安全提示且不触发 500
- [ ] **12.28. 导入预览是否不写库** — 预览是否展示总行数、可导入数量、错误数量、即将创建标签 / 创作者数量、前 5 条数据和错误行，且不会写入 SQLite
- [ ] **12.29. 错误行提示是否完整** — 缺文件、文件类型、CSV 空、缺表头、缺 title 映射、重复映射、缺标题、非法状态、非法评分、非法 extra JSON、JSON 格式、缺 items、items 非数组和导入异常是否有中英文友好提示
- [ ] **12.30. 部分错误导入是否明确** — 部分错误行时是否只导入有效行并显示错误摘要，全部错误时是否禁止确认导入
- [ ] **12.31. 导入结果摘要是否完整** — 是否展示成功导入、跳过、创建标签、创建创作者、标签关联、创作者关联、状态记录和错误数量
- [ ] **12.32. 导入实现是否本地事务安全** — 确认导入是否优先使用 SQLAlchemy ORM、本地 SQLite 和事务提交，失败时不破坏现有数据库
- [ ] **12.33. 导入增强 i18n 是否完整** — 新增导入模板、字段说明、映射、预览、错误行、结果摘要和错误提示是否同时覆盖中文 / English
- [ ] **12.34. Phase 2-A4 边界是否保持** — 是否未引入外部内容源、URL 导入、爬虫、adapter、远程图片拉取、自动同步、多源搜索、推荐系统、AI 助手、云同步、多用户系统或大型前端框架

## Phase 2-B1 响应式 UI 检查

- [ ] **12.35. 全站响应式基础是否清晰** — 主内容、卡片、网格、表单、按钮、标签和 flash message 在手机宽度下是否避免明显全站横向滚动
- [ ] **12.36. 顶部导航移动端是否可用** — NSFWTrack、语言切换、主要页面入口和登录 / 登出入口是否在窄屏下可见且不挤成一行
- [ ] **12.37. 列表页移动端是否可用** — 搜索、筛选、排序、page_size、清除筛选、批量编辑、条目卡片、多选 checkbox 和分页是否不挤压错位
- [ ] **12.38. 详情页移动端是否可用** — 基本信息、状态、标签、创作者、快速编辑和操作区是否清晰排列，删除按钮是否仍有危险样式和二次确认
- [ ] **12.39. 导入页面移动端是否可用** — 模板下载、字段说明、CSV 字段映射、预览、错误行和导入结果是否可读，表格溢出是否限制在局部容器
- [ ] **12.40. 表单页面移动端是否可用** — 条目、标签、创作者等表单的 input、textarea、select、保存 / 取消 / 删除按钮是否布局清晰
- [ ] **12.41. 备份 / 统计页面基础适配是否完成** — 备份导出 / 预览 / 恢复入口是否可点击，统计和管理表格是否使用局部滚动或不溢出
- [ ] **12.42. Phase 2-B1 边界是否保持** — 是否未新增业务功能、依赖、数据库结构、外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-B2 统计面板增强检查

- [ ] **12.43. 统计 service 是否只读本地 SQLite** — 是否集中生成统计结构，未请求外部网络，未读取外部数据源，未新增数据库字段或依赖
- [ ] **12.44. 总览卡片是否完整** — 是否展示总条目数、总标签数、总创作者数、有状态记录、有评分、平均评分、最近 7 天新增和最近 30 天新增
- [ ] **12.45. 状态分布是否完整安全** — 是否覆盖现有 6 个内部状态，显示数量和比例，空数据不除零，不新增状态值
- [ ] **12.46. 评分分布是否完整安全** — 是否只统计 1-5 分已有评分，展示数量、比例、平均、最高和最低评分，空数据有提示
- [ ] **12.47. 标签 / 创作者排行是否本地化** — 是否只展示本地前 10 个关联排行和占比，长名称不撑破布局，不做推荐或自动分析
- [ ] **12.48. 最近活动是否只基于本地时间字段** — 是否使用 `created_at` / `updated_at` 展示 7 / 30 天新增和更新，不做预测或时间线推荐
- [ ] **12.49. 数据完整性概览是否中性** — 是否只展示没有标签、创作者、状态、评分和描述的数量，不输出好坏评价，不自动修改数据
- [ ] **12.50. Phase 2-B2 边界是否保持** — 是否未引入外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、AI 分析、图表库、前端框架、构建流程、tag 或 GitHub Release

## Phase 2-C1 合集 / 清单管理检查

- [ ] **12.51. 合集模型是否本地化** — 是否只新增 `collections` 与 `item_collections` 本地 SQLite 表，未修改已有表字段，未新增依赖或 Alembic
- [ ] **12.52. 合集名称校验是否安全** — 名称是否去除首尾空格，空名称和重复名称是否有中英文友好错误提示
- [ ] **12.53. 合集删除是否安全** — 删除合集是否只删除合集和关联关系，不删除条目
- [ ] **12.54. 关联关系是否防重复** — 同一条目重复加入同一合集是否不会重复创建关联行
- [ ] **12.55. 移出不存在关联是否稳定** — 移出不存在的条目 / 合集关联是否不触发 500
- [ ] **12.56. 合集列表页是否完整** — 是否显示名称、描述、条目数量、创建 / 更新时间、详情、编辑、删除入口和空状态
- [ ] **12.57. 合集详情页是否完整** — 是否显示合集信息、合集内条目、空状态，并能添加 / 移出已有本地条目
- [ ] **12.58. 条目详情页是否显示合集** — 是否展示当前所属合集，并能加入一个已有合集或移出当前关联合集
- [ ] **12.59. 列表页合集筛选是否可组合** — 是否能与关键词、标签、创作者、状态、排序和分页组合，query string 是否保留
- [ ] **12.60. 批量合集操作是否限于当前页选择** — 是否只处理当前页勾选条目，未实现跨页全选或持久化选择状态
- [ ] **12.61. 统计页合集指标是否完整** — 是否展示总合集数、有合集条目数、无合集条目数和合集排行
- [ ] **12.62. 合集 i18n 是否完整** — 新增页面、按钮、flash、筛选、批量操作和统计文案是否同时覆盖中文 / English
- [ ] **12.63. Phase 2-C1 边界是否保持** — 是否未引入外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、云同步、多用户系统、前端框架、构建流程、tag 或 GitHub Release

## Phase 2-C2 合集备份 / 导入检查

- [ ] **12.64. JSON 备份是否包含合集表** — 导出的 JSON 是否包含 `collections` 和 `item_collections`，且未移除旧表
- [ ] **12.65. 旧 JSON 备份是否兼容** — 缺少 `collections` / `item_collections` 的旧备份是否仍可预览和恢复
- [ ] **12.66. JSON 备份预览是否完整** — 是否显示合集数量、条目-合集关联数量、即将创建 / 合并合集、可恢复 / 不可恢复关联和合集错误
- [ ] **12.67. JSON 恢复是否安全合并合集** — 是否按名称合并合集，不创建重复合集，不删除现有条目，不覆盖清空数据库
- [ ] **12.68. JSON 恢复关联是否防重复** — 重复 `item_collections` 是否不会重复创建，已存在关联是否安全跳过
- [ ] **12.69. JSON 恢复坏关联是否可审计** — 缺失条目、缺失合集、格式无效或数据不完整的合集关联是否跳过并记录错误，不触发 500
- [ ] **12.70. CSV 导出是否包含 collections** — CSV 表头是否包含 `collections`，多个合集是否使用分号分隔，字段名是否不翻译
- [ ] **12.71. CSV 导入是否支持 collections** — 可选 `collections` 字段是否能自动创建 / 关联本地合集，旧 CSV 缺少该字段是否仍可导入
- [ ] **12.72. JSON 导入是否支持 collections** — 可选 `collections` 是否必须为字符串数组，非数组或非字符串元素是否进入错误行
- [ ] **12.73. 导入预览是否不写库** — CSV / JSON 预览是否显示合集创建 / 关联 / 错误统计且不写入合集或关联
- [ ] **12.74. 导入结果摘要是否完整** — 是否显示创建合集、关联合集、跳过合集和 collections 字段错误数量
- [ ] **12.75. 导入模板是否更新** — CSV / JSON 模板是否包含 `collections` 示例且不包含外部 URL 或敏感信息
- [ ] **12.76. 合集备份 / 导入 i18n 是否完整** — 新增备份、恢复、导出、导入、预览、错误和结果文案是否同时覆盖中文 / English
- [ ] **12.77. Phase 2-C2 边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖，未引入外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-D1 重复条目检测与合并检查

- [ ] **12.78. 重复检测是否本地化** — 是否只读取本地 SQLite 数据，未请求外部网络，未接入外部内容源、URL 导入、爬虫、adapter、AI 或图片相似度
- [ ] **12.79. 标题完全匹配是否安全** — 是否只在检测时忽略首尾空格，不修改数据库原始标题，不自动合并
- [ ] **12.80. 标题归一化匹配是否可解释** — 是否使用 Unicode NFKC、trim、casefold / lower 和连续空白折叠，不引入新依赖或复杂模糊算法
- [ ] **12.81. 候选列表是否只读** — `/duplicates` 是否要求登录，展示匹配类型、匹配 key、候选数量、标题、状态、评分、标签 / 创作者 / 合集数量和对比入口，且不会写库
- [ ] **12.82. 对比页校验是否完整** — `/duplicates/compare` 是否要求登录，非法 ID、缺失条目和相同条目不会触发 500
- [ ] **12.83. 对比内容是否完整** — 是否展示 primary / duplicate 的标题、简介、状态、评分、短评、标签、创作者、合集、`extra JSON`、创建时间和更新时间
- [ ] **12.84. 合并是否只能手动确认** — 是否只允许 POST 合并，有危险提示、备份建议和浏览器确认，未实现 GET 合并、自动合并或批量自动合并
- [ ] **12.85. 合并主从关系是否明确** — 是否始终保留 primary item，成功后删除 duplicate item，失败时 rollback
- [ ] **12.86. 关系转移是否安全** — 标签、创作者和合集关系是否转移到 primary，不重复创建关系，且不删除标签、创作者或合集记录本身
- [ ] **12.87. 状态 / 评分 / 短评冲突是否可控** — primary 缺失时可复制 duplicate；双方都有值时默认保留 primary，只有用户显式选择才覆盖
- [ ] **12.88. 简介冲突是否可控** — primary 缺失时可复制 duplicate；双方都有值时默认保留 primary，只有用户显式选择才覆盖
- [ ] **12.89. extra JSON 合并是否安全** — 缺失时复制，双方都有值时浅合并非冲突键，冲突键默认保留 primary，坏 JSON 不触发 500 且结果为有效 JSON
- [ ] **12.90. 合并结果摘要是否清晰** — 是否展示 primary 标题、duplicate 标题、关系转移数量、字段处理结果、`extra` 合并数量、冲突保留数量和 duplicate 删除状态
- [ ] **12.91. 入口和移动端是否可用** — 导航、条目列表、条目详情是否有重复检测入口，长标题和窄屏布局不明显溢出
- [ ] **12.92. 重复合并 i18n 是否完整** — 新增候选、对比、确认、结果和错误文案是否同时覆盖中文 / English
- [ ] **12.93. Phase 2-D1 边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖，未引入外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-D2 元数据清理与合并检查

- [ ] **12.94. 元数据检测是否本地化** — 是否只读取本地 SQLite tags、creators、collections，未请求外部网络，未接入外部内容源、URL 导入、爬虫、adapter、AI 同义词识别或推荐系统
- [ ] **12.95. 名称完全匹配是否安全** — 是否只在检测时忽略首尾空格，不修改数据库原始名称，不自动合并
- [ ] **12.96. 名称归一化匹配是否可解释** — 是否使用 Unicode NFKC、trim、casefold 和连续空白折叠，不引入新依赖、AI 判断或复杂模糊算法
- [ ] **12.97. 清理候选页是否只读** — `/cleanup` 是否要求登录，展示重复标签 / 创作者 / 合集、匹配类型、匹配 key、对象名称、关联条目数量和对比入口，且不会写库
- [ ] **12.98. 元数据对比页校验是否完整** — `/cleanup/compare` 是否要求登录，非法 type、缺失对象和相同对象不会触发 500
- [ ] **12.99. 对比内容是否完整** — 是否展示 primary / duplicate 的类型、名称、关联条目数量、关联条目预览、危险提示和备份建议
- [ ] **12.100. 合集 description 冲突是否可控** — primary 缺失时可复制 duplicate；双方都有不同值时默认保留 primary，只有用户显式选择才覆盖
- [ ] **12.101. 合并是否只能手动确认** — 是否只允许 POST 合并，有危险提示、备份建议和浏览器确认，未实现 GET 合并、自动合并、一键全部合并或批量自动合并
- [ ] **12.102. 标签合并是否安全** — 是否保留 primary tag，转移 duplicate tag 的 item_tags，跳过重复关联，删除 duplicate tag，不删除任何条目或其他标签
- [ ] **12.103. 创作者合并是否安全** — 是否保留 primary creator，转移 duplicate creator 的 item_creators，跳过重复关联，删除 duplicate creator，不删除任何条目或其他创作者
- [ ] **12.104. 合集合并是否安全** — 是否保留 primary collection，转移 duplicate collection 的 item_collections，跳过重复关联，删除 duplicate collection，不删除任何条目、标签或创作者
- [ ] **12.105. 合并失败是否回滚** — primary 不存在、duplicate 不存在、type 无效或相同对象是否失败且不留下明显半写入状态，不展示完整异常堆栈
- [ ] **12.106. 合并结果摘要是否清晰** — 是否展示合并类型、保留对象、删除对象、转移关联数、跳过重复关联数、description 处理、duplicate 删除状态和重新查看清理页建议
- [ ] **12.107. 入口和移动端是否可用** — 登录后导航、标签页、创作者页、合集页是否有清理入口，长名称和窄屏布局不明显溢出
- [ ] **12.108. 元数据清理 i18n 是否完整** — 新增候选、对比、确认、结果和错误文案是否同时覆盖中文 / English，且不翻译 API 字段名或 NSFWTrack
- [ ] **12.109. Phase 2-D2 边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖，未引入外部内容源、URL 导入、爬虫、adapter、推荐系统、AI 助手、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-E1 保存筛选视图 / 常用视图检查

- [ ] **12.110. saved_views 表是否本地化** — 是否只新增 `saved_views` 本地 SQLite 表，未修改已有表字段，未删除已有表，未新增依赖或 Alembic
- [ ] **12.111. 旧数据库兼容是否成立** — 旧数据库启动是否能通过现有 `create_all` 自动创建 `saved_views`，旧数据不受影响
- [ ] **12.112. 保存参数是否白名单过滤** — 是否只保存现有条目列表支持的关键词、状态、标签、创作者、合集、最低评分、时间范围、排序和 page_size 参数
- [ ] **12.113. 分页页码是否不保存** — 是否明确丢弃 `page`，避免保存视图打开过期分页
- [ ] **12.114. 未知或危险参数是否丢弃** — 是否不保存 session、cookie、CSRF、完整外部 URL、站外跳转目标或未知 query 参数
- [ ] **12.115. query string 是否稳定** — 是否经过规范化和稳定排序，便于比较、测试和备份恢复
- [ ] **12.116. 创建 / 更新 / 删除是否安全** — 是否要求登录和 POST，空名称、重复名称、过长名称和非法 ID 不触发 500
- [ ] **12.117. 删除是否有确认** — 删除保存视图是否有浏览器确认，并且不会删除任何条目或筛选命中的数据
- [ ] **12.118. apply 是否只读** — 应用保存视图是否只 GET 重定向到 `/items`，不修改数据库，不允许站外跳转
- [ ] **12.119. 列表页交互是否完整** — 条目列表页是否包含保存当前视图、已保存视图列表、应用、更新和删除入口，并且不破坏现有筛选 / 排序 / 分页
- [ ] **12.120. 移动端是否可用** — 保存视图面板、名称输入、query string 展示和操作按钮在窄屏下是否不明显溢出
- [ ] **12.121. saved views i18n 是否完整** — 新增保存视图、常用视图、已保存视图、名称、保存、应用、更新、删除、确认和 flash 文案是否同时覆盖中文 / English
- [ ] **12.122. saved views 备份是否兼容** — JSON 导出 / 预览 / 恢复是否支持 `saved_views`，旧备份缺少该表时是否仍可预览和恢复
- [ ] **12.123. Phase 2-E1 边界是否保持** — 是否未引入 AI 推荐、智能分类、外部内容源、URL 导入、爬虫、adapter、云同步、多用户共享视图、复杂权限、tag 或 GitHub Release

## Phase 2-E2 最近访问 / 最近编辑检查

- [ ] **12.124. item_activity 表是否本地化** — 是否只新增 `item_activity` 本地 SQLite 表，未修改已有表字段，未删除已有表，未新增依赖或 Alembic
- [ ] **12.125. 每 item 单记录是否成立** — `item_id` 是否指向本地条目且唯一，条目删除后是否不会留下影响页面的坏记录
- [ ] **12.126. 最近访问记录是否受限** — 是否只在登录后的条目详情页访问记录 `last_viewed_at` / `view_count`，不记录列表曝光、不存在条目或未登录访问
- [ ] **12.127. 访问记录失败是否安全** — activity 写入失败是否不会导致详情页 500，不展示完整异常堆栈
- [ ] **12.128. 最近编辑触发点是否完整** — 条目基础信息、状态、评分、短评、标签、创作者、合集变更是否成功后记录 `last_edited_at` / `edit_count`
- [ ] **12.129. 批量编辑 activity 是否限于当前页选择** — 是否只记录当前 POST 实际处理到的条目，未实现跨页扩展或跨页全选
- [ ] **12.130. 导入 / 备份 / 导出是否不误记编辑** — 导入、备份、导出流程是否没有把每条记录写成最近编辑
- [ ] **12.131. `/activity` 页面是否安全** — 是否要求登录、只读展示最近访问和最近编辑、空状态稳定、非法数据不 500
- [ ] **12.132. 清空 activity 是否安全** — 是否只允许 POST、要求登录、有浏览器确认，只删除 `item_activity`，不删除条目、标签、创作者、合集或 saved views
- [ ] **12.133. 页面入口是否完整** — 首页或统计页是否展示最近访问 / 最近编辑，条目列表页是否提供快捷入口，详情页是否展示单条 activity 信息
- [ ] **12.134. activity i18n 是否完整** — 新增最近活动、最近访问、最近编辑、清空、确认、空状态、次数和时间文案是否同时覆盖中文 / English
- [ ] **12.135. activity 备份是否兼容** — JSON 导出 / 预览 / 恢复是否支持 `item_activity`，旧备份缺少该表时是否仍可预览和恢复，缺失条目引用是否跳过并记录错误
- [ ] **12.136. 隐私边界是否保持** — 是否未记录 IP、User-Agent、设备指纹、访问来源、站外 URL、第三方 analytics 或多用户活动流
- [ ] **12.137. Phase 2-E2 边界是否保持** — 是否未引入 AI 推荐、智能分析、自动分类、外部内容源、URL 导入、爬虫、adapter、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-E3 快捷操作入口 / 工作台增强检查

- [ ] **12.138. 首页工作台入口是否完整** — 是否包含新增条目、条目列表、保存视图、最近活动、统计、合集、重复条目、元数据清理、导入和备份入口
- [ ] **12.139. 首页 saved views 卡片是否只读** — 是否只读取本地 `saved_views` 并提供导航入口，不自动保存、更新或删除视图
- [ ] **12.140. 首页最近活动入口是否只读** — 是否仅展示最近访问 / 最近编辑和跳转入口，不因为首页曝光创建 activity
- [ ] **12.141. 条目列表页快捷入口是否完整** — 是否包含新增条目、保存当前视图、已保存视图、最近活动、重复条目检测、元数据清理、导入和备份入口
- [ ] **12.142. saved views 是否未回归** — 保存当前视图、应用、更新、删除和 query string 规范化是否仍按 Phase 2-E1 规则工作
- [ ] **12.143. 列表筛选是否未回归** — 关键词、状态、标签、创作者、合集、评分、时间范围、排序、分页和 page_size 是否仍可组合使用
- [ ] **12.144. 批量编辑是否未回归** — 当前页选择、批量修改、批量删除确认和安全 `next` 返回是否未被快捷区破坏
- [ ] **12.145. 快捷入口是否只导航** — 快捷区是否不包含 POST 表单、confirm、删除、合并、清空 activity、恢复备份或一键危险操作
- [ ] **12.146. 安全边界是否保持** — 快捷入口是否未绕过登录、POST-only 修改要求或危险操作确认提示
- [ ] **12.147. 移动端布局是否可用** — quick action grid 是否在窄屏下自动换行 / 单列，不造成明显横向溢出或挤压
- [ ] **12.148. i18n 是否完整** — 工作台、快捷操作、常用入口、最近访问、最近编辑、已保存视图、数据清理、重复条目、导入数据、备份数据、查看统计、管理合集、新增条目、查看全部和暂无数据是否同时覆盖中文 / English
- [ ] **12.149. 空数据是否稳定** — 没有 saved views、没有 activity、没有条目时首页和列表页是否不触发 500 并展示空状态
- [ ] **12.150. 数据库和依赖边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖或前端构建流程
- [ ] **12.151. Phase 2-E3 边界是否保持** — 是否未引入 AI 推荐、智能分析、自动分类、外部内容源、URL 导入、爬虫、adapter、云同步、多用户共享、第三方统计、活动趋势图表、tag 或 GitHub Release

## Phase 2-F1 数据健康检查 / 本地数据自检检查

- [ ] **12.152. 数据健康 service 是否只读** — 是否只读取本地 SQLite 现有表并生成报告，不执行用户输入 SQL、不写库、不 commit、不删除业务数据
- [ ] **12.153. `/data-health` 是否要求登录** — 未登录访问是否跳转登录，登录后是否能稳定渲染健康状态、摘要和明细
- [ ] **12.154. 页面是否无自动修复动作** — 页面是否没有自动修复、一键修复、自动删除核心实体、自动合并、自动导入、AI 判断或未确认危险 POST 入口
- [ ] **12.155. 条目基础检查是否完整** — 是否报告空标题、无效 `rating`、无效 `status`、缺失 / 异常创建更新时间、更新时间早于创建时间和异常 `extra` JSON
- [ ] **12.156. 关系完整性检查是否完整** — 是否报告 `item_tags`、`item_creators`、`item_collections` 中缺失 item、tag、creator 或 collection 的孤立关系
- [ ] **12.157. 重复关系检查是否完整** — 是否报告同一 item 重复关联同一 tag、creator 或 collection，且不自动去重
- [ ] **12.158. saved views 检查是否完整** — 是否报告空名称、空 / 异常 `query_string`、未知参数、`page` / `next` / `redirect` 和外部 URL
- [ ] **12.159. item activity 检查是否完整** — 是否报告缺失 item 引用、负数 `view_count` / `edit_count` 和异常 `last_viewed_at` / `last_edited_at`
- [ ] **12.160. 报告展示是否可审查** — 是否展示总体状态、问题总数、按类型分组数量、影响对象类型、影响对象 id、简短说明和细节
- [ ] **12.161. 备份提示是否明确** — 页面是否提示发现问题后先导出 JSON 备份，再进行人工修复
- [ ] **12.162. 入口是否安全** — 顶部导航或工作台入口是否只是 GET 导航，不破坏语言切换、登录 / 登出、saved views、activity、cleanup、duplicates、backup 或 import
- [ ] **12.163. i18n 是否完整** — 数据健康、健康 / 警告 / 问题、问题总数、问题明细、条目问题、关系问题、重复关系、saved views、activity、孤立关系、空标题、无效评分、无效状态、未知参数、建议备份和只读提示是否覆盖中文 / English
- [ ] **12.164. 测试是否覆盖只读边界** — 是否测试 `/data-health` 不修改数据库、不删除条目 / 标签 / 创作者 / 合集 / 关系 / saved views / activity / state 数据
- [ ] **12.165. 回归是否覆盖** — saved views、activity、cleanup、duplicates、backup、import、filters、bulk edit、login 和 i18n 测试是否仍通过
- [ ] **12.166. 数据库边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增 Alembic 或迁移机制
- [ ] **12.167. 依赖边界是否保持** — 是否未新增运行时或开发依赖，未引入前端构建流程
- [ ] **12.168. Phase 2-F1 禁止项是否保持** — 是否未引入外部信息查询、外部内容源、URL 导入、爬虫、adapter、AI 判断、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-F2 备份文件校验 / 导入 dry-run 增强检查

- [ ] **12.169. 备份校验 service 是否只读** — `backup_validator` 是否只解析上传 JSON 和读取当前数据库摘要，不写库、不 commit、不删除业务数据
- [ ] **12.170. 备份校验入口是否要求登录** — `/backup/preview` 和 `/api/backup/preview/json` 是否仍要求登录，未登录不会执行校验
- [ ] **12.171. 异常文件是否不 500** — 非 JSON、非法 JSON、空 JSON、非对象 JSON、schema 不匹配、缺少 tables 和缺字段是否显示友好错误或报告
- [ ] **12.172. 旧备份兼容是否保持** — 缺少 `collections`、`item_collections`、`saved_views` 或 `item_activity` 的旧备份是否不被视为致命错误
- [ ] **12.173. 备份结构检查是否完整** — 是否覆盖未知顶层字段、未知表、未知字段、必填字段、重复 id、空标题、空名称、无效 `extra` JSON、无效 `status` 和无效 `rating`
- [ ] **12.174. 备份关系检查是否完整** — 是否报告 `item_tags`、`item_creators`、`item_collections` 的孤立条目、孤立目标和重复关系，且不自动修复
- [ ] **12.175. saved views / activity 校验是否完整** — 是否报告异常 query string、外部 URL、`page` / `next` / `redirect`、未知参数、缺失 item、重复 activity 和负数计数
- [ ] **12.176. 恢复 dry-run 是否只是报告** — 是否展示预计读取表数量、关系数量、预计跳过数量、warning 和备份建议，但不恢复、不创建、不更新、不删除
- [ ] **12.177. 导入 dry-run 是否增强** — CSV / JSON 预览是否展示 error / warning / info、可导入行、跳过行、未知字段、无效评分 / 状态、异常 tags / creators 和重复标题候选
- [ ] **12.178. 正式导入流程是否未被破坏** — 现有 CSV / JSON 正式导入、字段映射、部分错误行导入和结果摘要是否仍按原语义工作
- [ ] **12.179. 报告展示是否可审查** — 页面是否显示总体状态、数量、影响数据类型、行号 / 对象 id、说明、细节和写入前备份提示，移动端长文本不撑破布局
- [ ] **12.180. i18n 是否完整** — 备份校验、恢复 dry-run、导入 dry-run、可继续 / 有警告 / 不建议继续、错误 / 警告 / 信息、未知字段、缺失字段、文件过大、JSON 解析失败、只读提示和返回提示是否覆盖中文 / English
- [ ] **12.181. 测试是否覆盖只读边界** — 是否测试 dry-run 不写数据库、不删除条目 / 标签 / 创作者 / 合集 / 关系 / saved views / activity / state 数据
- [ ] **12.182. 数据库和依赖边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖或 Alembic
- [ ] **12.183. Phase 2-F2 禁止项是否保持** — 是否未引入自动修复、一键修复、自动删除、自动导入、自动恢复、自动合并、外部信息查询、外部内容源、URL 导入、爬虫、adapter、AI 判断、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-F3 数据健康手动修复 / 低风险维护操作检查

- [ ] **12.184. 修复 service 是否白名单分派** — `data_health_fixes` 是否只接受允许的 `fix_type`，不接受用户传入表名、列名、SQL 或任意操作名
- [ ] **12.185. 允许修复范围是否严格** — 是否只修复孤立 / 重复 `item_tags`、`item_creators`、`item_collections`，孤立 `item_activity`，负数 activity 计数和 saved views query 参数
- [ ] **12.186. 核心实体是否绝不删除** — 修复逻辑是否不执行针对 `items`、`tags`、`creators`、`collections` 的 delete，不修改这些核心实体内容
- [ ] **12.187. `/data-health` GET 是否继续只读** — GET 页面是否只构建报告和可用修复入口，不写库、不 commit、不执行修复
- [ ] **12.188. 修复入口是否要求登录和 POST** — 未登录 POST 是否跳转登录，GET `/data-health/fix` 是否不能执行修复
- [ ] **12.189. confirm 是否双层存在** — 页面按钮是否有浏览器确认，服务端是否要求 `confirm=1`
- [ ] **12.190. 是否没有一键修复全部** — 页面是否无 fix-all 表单，服务端是否拒绝 `fix_all` 且不 500
- [ ] **12.191. 非法 `fix_type` 是否友好失败** — 未知类型是否返回错误 flash，不展示异常堆栈，不修改数据
- [ ] **12.192. rollback 是否可靠** — handler 异常后是否 rollback，半写入删除不会保留
- [ ] **12.193. 孤立关系清理是否正确** — 是否只删除指向缺失 item / tag / creator / collection 的关系行
- [ ] **12.194. 重复关系清理是否正确** — 对旧 schema 重复关系是否每组保留一条关系行，删除多余关系行
- [ ] **12.195. activity 修复是否正确** — 是否只删除孤立 `item_activity`，并将负数 `view_count` / `edit_count` 修正为 `0`
- [ ] **12.196. saved views 参数修复是否正确** — 是否只规范化 `saved_views.query_string`，移除 `page` / `next` / `redirect`、未知参数和外部 URL 值，不删除 saved view
- [ ] **12.197. 修复摘要是否可审查** — 页面是否显示删除 / 修正 / 跳过数量，且提示先做 JSON 备份
- [ ] **12.198. i18n 是否完整** — 修复入口、按钮、确认、结果、错误、备份提示和各 `fix_type` 标签 / 描述是否同时覆盖中文 / English
- [ ] **12.199. 测试是否覆盖安全边界** — 是否覆盖未登录、GET、非法类型、`fix_all`、缺少 confirm、核心实体保留和 rollback
- [ ] **12.200. 数据库和依赖边界是否保持** — 是否未新增数据库表、未修改已有字段、未新增依赖或 Alembic
- [ ] **12.201. Phase 2-F3 禁止项是否保持** — 是否未引入自动修复全部、自动合并、AI 判断、外部内容源、URL 导入、爬虫、adapter、云同步、多用户系统、tag 或 GitHub Release

## Phase 2-G1 基础设置中心检查

- [ ] **12.202. `app_settings` 表是否本地化** — 是否只新增本地 SQLite `app_settings` 表，未修改已有表字段，未新增依赖或 Alembic
- [ ] **12.203. 设置 key 是否白名单** — 是否只允许 `default_language`、`default_page_size`、`default_sort`、`default_sort_dir` 和 `default_home`
- [ ] **12.204. 设置 value 是否白名单** — 是否只允许既定语言、分页大小、排序字段、排序方向和首页入口，不保存外部 URL、脚本内容或任意字符串
- [ ] **12.205. `/settings` 是否要求登录** — 未登录访问是否跳转登录，登录后是否能稳定渲染当前设置
- [ ] **12.206. 保存是否只能 POST** — 设置保存是否只通过 `POST /settings`，非法 key/value 是否友好失败、不 500、不半写入
- [ ] **12.207. reset 是否确认** — 恢复默认设置是否只通过 `POST /settings/reset`，服务端是否要求 `confirm=1`
- [ ] **12.208. 默认分页是否生效且可覆盖** — `/items` 没有 `page_size` 时使用本地默认值，显式 URL `page_size` 优先
- [ ] **12.209. 默认排序是否生效且可覆盖** — `/items` 没有 `sort` 时使用默认字段 / 方向，显式 URL `sort` 优先
- [ ] **12.210. 默认语言优先级是否正确** — session 显式语言选择是否优先于本地默认语言，不破坏 `/set-language`
- [ ] **12.211. 默认首页入口是否安全** — 首页是否只展示或高亮本地入口，不保存外部 URL，不执行自动跳转或危险操作
- [ ] **12.212. saved views 是否未被覆盖** — 已保存视图中的 query string 是否保持独立，不被新默认分页 / 排序改写
- [ ] **12.213. JSON 备份是否包含设置** — 新 JSON 备份是否包含 `app_settings`，且旧备份缺少该表时仍能预览 / 恢复
- [ ] **12.214. 备份恢复设置是否校验** — 恢复 `app_settings` 时是否复用 key/value 白名单，无效设置是否跳过或报告，不写入未知设置
- [ ] **12.215. i18n 是否完整** — 设置页、flash、导航、首页默认入口、备份校验中 `app_settings` 文案是否覆盖中文 / English
- [ ] **12.216. 测试是否覆盖关键路径** — 是否覆盖登录保护、合法保存、非法 key/value、默认分页、默认排序、显式 URL 覆盖、语言切换、reset、首页高亮和备份兼容
- [ ] **12.217. 回归是否覆盖** — items、saved views、backup、i18n、database 和 Docker 验收是否仍通过
- [ ] **12.218. 本地单用户边界是否保持** — 是否未加入多用户设置、外部账号、云同步、插件系统、权限矩阵或外部内容源
- [ ] **12.219. Phase 3 禁止项是否保持** — 是否未引入 URL 导入、爬虫、adapter、推荐系统、AI 助手、自动同步或远程图片拉取
- [ ] **12.220. 发布边界是否保持** — 是否未修改已发布 tag，未创建 GitHub Release

## Phase 2-G6 危险操作偏好与确认流程统一检查

- [ ] **12.221. 数据库边界是否保持** — 是否复用 `app_settings`，未新增表、未修改已有字段、未新增依赖
- [ ] **12.222. G6 key/value 是否白名单** — 是否只允许三个 G6 key 及 `standard` / `strict`、`always` / `dangerous_only`、`summary` / `detailed`
- [ ] **12.223. 禁用值是否拒绝** — `off`、`disabled`、`never`、未知 key、外部 URL、脚本或任意值是否拒绝且不半写入
- [ ] **12.224. standard 是否保持既有安全层** — 登录、写方法、浏览器 confirm、现有服务端确认和 rollback 是否均保留
- [ ] **12.225. strict 是否服务端验证** — 所有覆盖入口缺少或错误 `CONFIRM` 是否拒绝且不写库，精确文本是否可执行
- [ ] **12.226. 设置异常是否安全回退** — 非法行或读取异常是否回退到 `standard`，而不是无确认
- [ ] **12.227. 删除入口是否覆盖** — 条目、当前页批量条目、标签、创作者和合集删除是否使用统一策略
- [ ] **12.228. 合并与维护入口是否覆盖** — 条目 / 元数据合并、活动清空、备份恢复、数据健康修复、设置 reset 是否使用统一策略
- [ ] **12.229. GET 是否只读** — 所有危险操作 GET 是否不能执行写入，API 恢复 strict 是否同样服务端校验
- [ ] **12.230. 安全提示是否完整** — 页面是否显示对象、后果、删除范围、可恢复性、适用备份建议和当前确认模式
- [ ] **12.231. 备份提醒是否不可关闭** — `dangerous_only` 是否只收窄备份建议而不隐藏危险提示，是否不存在关闭值
- [ ] **12.232. 结果详情是否仅影响展示** — summary / detailed 是否不改变合并、删除、恢复或修复的数据结果和范围
- [ ] **12.233. rollback 与范围是否保持** — 确认失败是否发生在写入前，原服务失败 rollback 和当前页范围是否未改变
- [ ] **12.234. 备份兼容是否完整** — 新备份是否包含 G6 设置，旧备份缺少设置时是否使用安全默认值
- [ ] **12.235. i18n 与测试是否完整** — 新设置、提示、错误和结果文案是否中英对称，关键安全分支和全量回归是否通过
- [ ] **12.236. 禁止项是否保持** — 是否无一键全部删除 / 合并 / 修复、自动执行、外部源、AI、云同步、多用户、tag 或 GitHub Release

## Phase 2-H1 数据库版本记录与升级预检检查

- [ ] **12.237. 内部表范围是否准确** — 是否只新增 `schema_migrations`，且未修改、删除或重建任何现有业务表和字段
- [ ] **12.238. 版本表设计是否稳定** — `version` 是否唯一，`name` / `applied_at` 是否必填，当前版本是否来自代码常量
- [ ] **12.239. 新数据库是否安全登记** — 空数据库是否创建当前结构并在初始化成功后登记基线版本 `1`
- [ ] **12.240. 旧数据库是否先预检** — 无版本表的旧库是否先核对全部必要业务表和列，结构异常时不盲目登记
- [ ] **12.241. 旧业务数据是否保留** — 基线登记是否不清空、不重建、不修改现有业务数据
- [ ] **12.242. 当前版本是否幂等** — 已登记当前版本时是否正常启动且不增加重复记录
- [ ] **12.243. 高版本是否拒绝** — 数据库版本高于应用时，lifespan 是否拒绝继续并给出不含敏感路径的备份 / 兼容提示
- [ ] **12.244. 低版本是否只提示** — 数据库版本低于应用时是否标记需要升级，且不自动迁移、不改版本、不降级
- [ ] **12.245. 无法确认是否安全失败** — 空版本表、缺字段或读取异常是否按无法确认处理，而不是登记或绕过
- [ ] **12.246. 失败是否无错误记录** — 建表或登记失败时是否不留下错误 schema 版本行，不修改业务数据
- [ ] **12.247. 状态展示是否完整** — 设置页是否显示应用版本、数据库版本、正常 / 需升级 / 应用过旧 / 无法确认、最近登记时间和备份提示
- [ ] **12.248. 状态页是否登录且只读** — GET 是否要求登录，页面是否无版本输入、修改、降级或跳过按钮
- [ ] **12.249. 用户输入是否无法改版本** — URL 参数、设置表单、未知 key 和不存在的写路由是否不能修改 `schema_migrations`
- [ ] **12.250. 备份导出是否隔离** — JSON 备份表清单是否不包含 `schema_migrations`
- [ ] **12.251. 备份恢复是否隔离** — 旧备份缺少内部表是否兼容，伪造内部表数据是否被忽略且不能覆盖本地版本
- [ ] **12.252. 范围与回归是否保持** — 是否无真实迁移、自动修复、Alembic、新依赖、tag、Release，并通过 i18n、全量测试和 Docker 验收

## Phase 2-H2 显式迁移框架与升级 dry-run 检查

- [ ] **12.253. 生产注册表是否真实** — 是否没有为测试虚构生产迁移，`MIGRATION_REGISTRY` 是否为空，`CURRENT_SCHEMA_VERSION` 是否仍为 `1`
- [ ] **12.254. 迁移步骤契约是否完整** — 每步是否包含 from / to / name / preview / apply / pre-check / post-check，且只能由代码构造
- [ ] **12.255. 注册表是否拒绝坏链** — 是否拒绝重复源 / 目标、断层、跳级、倒序、循环、非法版本和无效 callback
- [ ] **12.256. 解析顺序是否正确** — 是否先从 `schema_migrations` 读取当前版本，再解析到应用常量目标版本的连续路径
- [ ] **12.257. 是否不提前要求最新结构** — 低版本启动和升级页是否不查询 / 校验可能尚不存在的最新业务表，源结构由对应迁移 pre-check 负责
- [ ] **12.258. 状态处理是否完整** — 当前版本、可升级、缺少路径、应用过旧和无法确认是否有明确只读状态
- [ ] **12.259. GET 是否绝对不迁移** — `GET /schema-upgrade` 和错误方法是否不调用 apply、不写业务数据、不写版本记录
- [ ] **12.260. preview 是否登录且只读** — `POST /schema-upgrade/preview` 是否要求登录，并使用 SQLite query-only、authorizer 和 rollback 阻止 DML / DDL
- [ ] **12.261. dry-run 报告是否完整** — 是否显示当前 / 目标版本、步骤顺序、预计变化、pre-check、warning / error、可升级状态和备份提示
- [ ] **12.262. 后续 pre-check 是否诚实** — 未应用前一步时是否将后续检查标记 deferred，而不是伪造通过；apply 是否逐步真实检查
- [ ] **12.263. apply 确认是否完整** — 是否要求登录、POST、浏览器 confirm、服务端原有确认、备份知情确认，strict 是否精确验证 `CONFIRM`
- [ ] **12.264. 用户输入是否封闭** — 表单和 handler 是否不接受 SQL、表名、目标版本、迁移名、降级或跳过检查参数
- [ ] **12.265. apply 是否重读版本** — 是否在写事务内重新读取当前版本并解析代码路径，避免依赖过期 preview
- [ ] **12.266. 事务是否覆盖整条链** — 所有 step apply、post-check 和版本 insert 是否在同一事务提交
- [ ] **12.267. 中途失败是否全回滚** — 两步链第二步失败时，第一步的 DML、DDL 和版本记录是否全部撤销
- [ ] **12.268. post-check 失败是否回滚** — 目标检查失败是否不留下结构变化、业务变化或新版本记录
- [ ] **12.269. 降级与自动迁移是否不存在** — 是否拒绝高到低路径，lifespan 是否只预检而不调用迁移注册表
- [ ] **12.270. 备份隔离是否保持** — `schema_migrations` 是否仍不导出 / 恢复，旧备份是否不能覆盖本地版本
- [ ] **12.271. 数据库与依赖边界是否保持** — 是否未修改现有业务表 / 字段、未新增表、依赖、Alembic 或前端构建流程
- [ ] **12.272. 文档与回归是否完整** — README / TASKS / REVIEW / CHANGELOG / PLAN 是否只记录 Unreleased，i18n、全量测试和 Docker 是否通过，tag / Release 是否未改

## Phase 2-I1 性能基线与数据库查询审查检查

- [ ] **12.273. 性能数据是否隔离** — 是否只创建临时 SQLite 数据库，未打开、修改或降级默认 schema 2 数据卷，运行后是否清理生成数据
- [ ] **12.274. 审查是否只读** — 审查连接是否启用 SQLite `query_only`，写语句是否在执行前拦截，成功和异常后是否恢复连接状态
- [ ] **12.275. 输入边界是否封闭** — CLI 是否只接受正整数规模和可选输出路径，不接受用户 SQL、表名、查询片段或生产数据库 URL
- [ ] **12.276. 数据规模是否完整** — 是否实际运行 100 / 1,000 / 10,000 条条目及配套标签、创作者、合集、saved views、activity 和关系数据
- [ ] **12.277. 页面范围是否完整** — 是否覆盖列表、工作台、统计、标签、创作者、合集、saved views、activity、duplicates、cleanup 和 data-health
- [ ] **12.278. 文件流程是否完整** — 是否覆盖真实备份 preview / validation 组合路径和 JSON 导入 dry-run，确认解析 / 遍历次数和查询数量
- [ ] **12.279. 证据是否完整** — 是否记录每个操作的数据规模、SQL 数量、耗时、重复指纹、查询计划、全表扫描、bounded 状态和 N+1 判定
- [ ] **12.280. N+1 判定是否有证据** — 是否用重复单行 SQL 次数与父对象数量对应来确认合集详情 N+1，并区分每 500 行分批的 selectin 查询放大
- [ ] **12.281. 分页判定是否真实** — 是否确认 item 结果保持 20 / 50 条分页，同时如实记录筛选元数据和 saved views 的无界加载
- [ ] **12.282. 性能测试是否稳定** — 是否不把固定毫秒数作为通过条件，而测试只读边界、操作覆盖、查询数增长上限和已确认 N+1
- [ ] **12.283. 结论是否克制** — `PERFORMANCE.md` 是否只把实测或静态确认的问题写成结论，并把未实施索引写为建议
- [ ] **12.284. 迁移边界是否保持** — 是否未新增索引、表、字段、真实迁移，未提升 schema 版本或向生产注册表添加迁移
- [ ] **12.285. 功能边界是否保持** — 是否未直接优化业务查询、改页面行为、新增依赖、缓存、后台任务、外部监控或前端框架
- [ ] **12.286. 文档与回归是否完整** — README / TASKS / REVIEW / CHANGELOG / PLAN / PERFORMANCE 是否记录 Unreleased，且全量测试和隔离 Docker 验收通过

## Phase 2-I2 查询优化与分页收敛检查

- [ ] **12.287. Items 加载范围是否正确** — 是否只为当前分页结果加载模板需要的 tags / creators / collections / state，且筛选、排序、分页结果与优化前一致
- [ ] **12.288. Filter metadata 是否保持完整** — tag / creator / collection 选项是否仍完整可用，同时不再递归加载关联 item graph
- [ ] **12.289. Cleanup 候选是否轻量且等价** — 候选页是否只取 id / name / relation count，exact / normalized 分组、primary 顺序和 compare URL 是否不变
- [ ] **12.290. Collection N+1 是否消失** — 是否没有按成员逐条加载 collection 的 SQL，且性能审计明确报告无 N+1
- [ ] **12.291. Collection 双分页是否安全** — members 和 available items 是否各限 20 条、显示完整总数、支持 available title 搜索且 GET 不修改成员关系
- [ ] **12.292. Metadata 分页是否完整** — tags / creators / collections 是否各限 50 条，非法 / 越界 page 是否安全回退且后续行可访问
- [ ] **12.293. Candidate 分页是否完整** — duplicates / cleanup 是否按 comparison pair 分页，每个 duplicate 均可访问且 compare / merge 语义不变
- [ ] **12.294. Data-health 总数是否准确** — 页面明细是否最多 200 条，同时 total、分类、code 和 fix option 数量仍基于完整问题集合
- [ ] **12.295. Settings 是否单次读取** — items / workbench 及共享 context 是否复用同一 AppSettings，单请求不重复查询
- [ ] **12.296. Stats 是否等价收敛** — overview、分布、排行、活动日桶和完整性结果是否保持，查询数是否由 28 降至不高于 11
- [ ] **12.297. 原有行为是否回归** — saved views、筛选、排序、批量编辑、合并、备份、导入、登录、POST、confirm 和 rollback 是否全量通过
- [ ] **12.298. 性能矩阵是否重跑** — 是否实际运行 100 / 1,000 / 10,000 三档，并在 PERFORMANCE.md 保留 I1 / I2 可审计对照
- [ ] **12.299. Schema 边界是否保持** — 是否无索引、表、字段、生产迁移、schema 版本、依赖、缓存或外部服务变化
- [ ] **12.300. 发布边界是否保持** — 是否只写 Unreleased，未修改旧 tag，未创建 GitHub Release，并使用隔离数据与 Docker volume

## Phase 2-I3 异常处理、日志与错误页面统一检查

- [ ] **12.301. 状态码是否完整** — 400 / 403 / 404 / 405 / 409 / 422 / 500 是否保持原状态而不是被统一成 200 或 500
- [ ] **12.302. HTML 错误页是否统一** — 页面请求是否使用双语模板、通用提示、request_id 和安全首页入口
- [ ] **12.303. JSON envelope 是否兼容** — API 是否返回 error / message / request_id，并为预期错误保留现有 detail 字段
- [ ] **12.304. 内容协商是否正确** — `/api/` 和明确 JSON Accept 是否返回 JSON，普通页面和 HTMX 请求是否保持 HTML
- [ ] **12.305. 404 是否独立** — 未匹配页面和 API 是否返回 404，且不触发系统异常或 error 级日志
- [ ] **12.306. 405 是否完整** — 自动和显式 405 是否保留正确的 Allow 响应头
- [ ] **12.307. 422 是否兼容安全** — 是否保留 type / loc / msg 和状态码，同时不回显 input、密码或上传内容
- [ ] **12.308. Request ID 是否安全** — 是否只接受标准 UUID / UUID hex，凭据外形或其他值替换，所有响应头与错误 body 是否一致
- [ ] **12.309. 500 是否最小暴露** — 是否只返回通用提示和 request_id，不返回 traceback、异常值、SQL、路径、环境或凭据
- [ ] **12.310. 请求日志字段是否完整** — 是否包含 request_id / method / 安全 path / status / duration，异常时仅增加 exception type
- [ ] **12.311. 日志是否脱敏** — 是否不记录 query、Authorization、Cookie、表单密码、上传内容、异常消息或 traceback
- [ ] **12.312. 重复日志是否关闭** — Uvicorn 原始 request-line access log 是否禁用，避免绕过应用路径脱敏
- [ ] **12.313. 预期错误是否分级正确** — 普通 4xx 是否使用 info，不作为系统崩溃记录
- [ ] **12.314. 高风险 rollback 是否保持** — 备份、导入、合并、健康修复、设置和 schema 升级失败是否仍无半写入
- [ ] **12.315. 安全确认是否保持** — 登录、POST、浏览器 confirm、服务端确认和 strict CONFIRM 是否未削弱
- [ ] **12.316. 范围是否保持** — 是否无外部日志、遥测、监控依赖、数据库结构、已发布 tag 或 Release 变化
- [ ] **12.317. 凭据外形 ID 是否隔离** — `ghp_` / `github_pat_` 等字符串是否在写响应和日志前替换，原值不可回显
- [ ] **12.318. 未匹配路径是否隔离** — 404 等未匹配路由是否固定记录 `/[unmatched]`，匹配路由是否只记录 route template

## Phase 2-I4 安全、兼容性与发布冻结检查

- [ ] **12.319. 路由认证是否完整** — 除登录和语言切换外，所有页面与 API 是否均有 session 登录保护
- [ ] **12.320. 写方法边界是否完整** — 所有 POST / PUT / DELETE 是否登录保护，是否不存在 GET 业务数据写操作和遗漏的 PATCH 路由
- [ ] **12.321. 同源防护是否有效** — unsafe 请求的 Origin / Referer 是否必须同源，跨站请求是否 403，headerless 本地 API 是否保持兼容
- [ ] **12.322. Session fixation 是否阻断** — 登录是否清除认证前 session，仅保留语言偏好并建立新认证代次
- [ ] **12.323. 登出重放是否阻断** — 登出或应用重启后，先前复制的已签名认证 cookie 是否无法继续访问
- [ ] **12.324. Cookie 属性是否安全** — 是否至少 HttpOnly / SameSite=Lax，并可通过 SESSION_COOKIE_SECURE 在直接 HTTPS 环境启用 Secure
- [ ] **12.325. 服务端确认是否存在** — 危险页面操作是否在 JavaScript confirm 之外验证 confirm 标记，strict 是否继续精确验证 CONFIRM
- [ ] **12.326. 跳转是否仅限站内** — next 和语言跳转是否拒绝绝对 URL、协议相对、反斜杠、编码反斜杠和控制字符
- [ ] **12.327. 输出是否安全** — 标题、名称、搜索和短评是否由 Jinja 默认转义，错误响应是否不含提交值和内部信息
- [ ] **12.328. 输入失败是否有界** — 非 JSON、损坏 JSON、非对象 JSON、非法字段与超大上传是否安全失败且不写库
- [ ] **12.329. 导入上限是否前置** — CSV / JSON 导入是否最多读取 limit+1 字节，超限是否在解析和事务前拒绝
- [ ] **12.330. 数据完整性是否保持** — 删除、批量、合并、健康、设置、导入、恢复和升级失败是否完整 rollback
- [ ] **12.331. Preview 是否只读** — 导入 / 备份 / schema dry-run 是否保持无写入，迁移 preview 是否继续 query_only 与 authorizer
- [ ] **12.332. 五类数据库是否通过** — 全新、旧无版本、合法 Schema 1、低版本和隔离高版本是否符合预检与不降级边界
- [ ] **12.333. 兼容性是否完整** — 中英文、URL 参数、备份格式、API / CSV / JSON 字段、空数据、分页和设置优先级是否保持
- [ ] **12.334. 日志边界是否保持** — request_id、固定 unmatched path、route template、405 / 422 / 500 与敏感字段排除是否回归通过
- [ ] **12.335. 性能是否无回归** — 三档矩阵是否保持 items 11、filtered 11、cleanup 4、collection detail 9、duplicates 7、metadata 3、stats 11 且无 N+1
- [ ] **12.336. Schema 与范围是否冻结** — 是否保持版本 1、空生产迁移注册表、无索引 / 表 / 字段 / 依赖 / 产品功能变化
- [ ] **12.337. 环境是否隔离** — 是否只用临时数据库和 Docker volume，未接触默认 schema 2 数据卷，并在验收后清理
- [ ] **12.338. 发布边界是否保持** — 是否只更新 Unreleased，未修改旧 tag / Release，未创建 v1.0.0 tag 或 Release

## Phase 2-K1 开发完成度审计检查

- [ ] **12.339. 审计是否只读** — 是否仅修改 COMPLETION_AUDIT / README / PLAN / TASKS / REVIEW / CHANGELOG / GOAL，未修改业务代码、依赖、Schema 或迁移
- [ ] **12.340. 占位扫描是否辨真伪** — 是否区分 TODO / stub 与继承类、异常类、SQLAlchemy Base、模板 placeholder 等正常结构
- [ ] **12.341. 入口是否完整** — 95 个路由、静态模板链接、表单 action 和 fragment 是否均有目标，是否没有 501 或失效入口
- [ ] **12.342. 文档承诺是否核对** — 核心功能是否有实现 / 测试证据，HTMX、升级说明和本地封面路径等不一致是否如实记录
- [ ] **12.343. 本地素材边界是否识别** — 是否记录 cover_path 可接受外部 URL、本地路径无挂载且缺测试的 P0 问题
- [ ] **12.344. 确认缺口是否识别** — 是否记录非删除批量写、状态清除和关系解除与 RULE.md 确认要求不一致
- [ ] **12.345. 凭据部署门禁是否识别** — 是否记录 `.env.example` 已知占位密码 / Secret 可启动的问题
- [ ] **12.346. F4 结论是否准确** — 是否确认数据健康备份提示、影响范围、单项修复、confirm / strict / rollback 已实现，并仅保留专项测试收口
- [ ] **12.347. 测试缺口是否分级** — 是否区分已完成的 K2 自动回归、授权后才可做的 K3 / N100 操作验收和可延后维护
- [ ] **12.348. 当时状态是否收敛** — PLAN / TASKS 是否记录 v1.0.4 为该阶段稳定版本、代码 / WSL 已完成，且不把未授权的 N100 部署列为开发任务
- [ ] **12.349. 禁止项是否保持** — 是否未引入外部内容、URL 导入、远程图片代理、AI、云同步、多用户、前端重写或虚构迁移

## Phase 2-K2 投入使用前边界收口检查

- [ ] **12.350. 本地素材契约是否唯一** — `cover_path` / `avatar_path` 是否只接受 `/media/` 下受支持图片，拒绝外部 URL、协议相对路径、data URL、穿越、反斜杠、编码分隔符、query 和 fragment
- [ ] **12.351. 本地素材是否真实可读且受保护** — `data/media` 是否由现有 Docker data 卷持久化，合法文件是否登录后 200，匿名访问是否跳转登录
- [ ] **12.352. 存量渲染是否安全** — 列表、详情和合集详情是否在输出 `<img src>` 前重新验证，旧外部值是否不会触发浏览器请求
- [ ] **12.353. 备份恢复是否同策** — backup validation / preview / restore 是否使用同一本地素材规则，非法路径是否在事务前阻断且无部分写入
- [ ] **12.354. 全部批量写是否确认** — status / rating / tag / collection / delete 是否都有浏览器提示、服务端 `confirm=1` 和 strict 精确 `CONFIRM`
- [ ] **12.355. 清除与解除是否确认** — 状态清除、tag / creator / collection 解除及合集侧移出是否统一在写入前确认，实体本身是否保留
- [ ] **12.356. 凭据占位值是否 fail closed** — `.env.example` 两个精确占位值是否拒绝启动，错误是否不回显值，有效自定义值是否正常
- [ ] **12.357. F4 专项是否闭合** — 中英文完整事实、备份链接、dangerous_only / always、strict 不弱化和无问题无修复控件是否有测试
- [ ] **12.358. 运维清单是否可执行** — README 是否包含首次安装、v0.9/v1.0 升级、停机 SQLite 备份、显式迁移和回滚步骤
- [ ] **12.359. 范围与验收是否保持** — 是否无新依赖、Schema、迁移、tag、Release 或外部请求，且 347 tests 与隔离 Docker 验收通过并清理

## Phase 2-L1 测试依赖兼容性收口检查

- [ ] **12.360. 警告根因是否确认** — 是否复现为 Starlette TestClient 在缺少 `httpx2` 时的 `StarletteDeprecationWarning`
- [ ] **12.361. 是否最小依赖修复** — 是否仅将开发 / CI 依赖改为 `httpx2`，未改业务代码、Schema、迁移或运行时 `requirements.txt`
- [ ] **12.362. 是否未掩盖警告** — 是否未使用 pytest filter / warnings ignore 隐藏弃用信息
- [ ] **12.363. 全量测试是否清洁** — `pytest` 是否 347 passed 且不再出现 httpx / httpx2 弃用警告
- [ ] **12.364. Docker 验收是否通过** — 隔离 build / up / `/login` 200 / down 与清理是否完成
- [ ] **12.365. 文档是否更新** — README / TASKS / REVIEW / CHANGELOG Unreleased / GOAL 是否记录 L1

## Phase 2-L2 直接依赖版本基线检查

- [ ] **12.366. 是否只固定直接依赖** — requirements 是否仅钉直接依赖，未生成完整传递锁文件
- [ ] **12.367. 是否基于已验证版本** — 钉定版本是否来自当前 Python 3.12 已通过测试的环境，而非盲目升级
- [ ] **12.368. CI 是否 pip check** — workflow 是否在 pytest 前运行 `pip check`
- [ ] **12.369. 全新安装是否通过** — 全新 venv 安装、`pip check`、全量测试是否通过
- [ ] **12.370. Docker 是否通过** — 隔离 build / up / `/login` / down 是否通过
- [ ] **12.371. 业务边界是否保持** — 是否未改业务代码、数据库、Schema 或迁移

## Phase 2-L3 浏览器安全响应头基线检查

- [ ] **12.372. 是否统一附加** — 成功 HTML、重定向、JSON、404/422/405 和媒体响应是否都有安全头
- [ ] **12.373. 头集合是否最小兼容** — 是否包含 nosniff、Referrer-Policy、DENY frame、受限 Permissions-Policy
- [ ] **12.374. 是否避免破坏性策略** — 是否未启用 HSTS，未加入会破坏内联脚本/样式的激进 CSP
- [ ] **12.375. 兼容性是否保持** — `X-Request-ID`、405 `Allow`、登录、表单确认和本地媒体是否无回归
- [ ] **12.376. 验收是否完整** — 专项测试、全量测试、`pip check` 和隔离 Docker 是否通过
- [ ] **12.377. 范围是否保持** — 是否未改业务逻辑、数据库、Schema、依赖或版本

## Phase 2-L4 CI Docker 冒烟验收检查

- [ ] **12.378. 是否独立 job** — Docker 冒烟是否与 pytest 分开，且保留 pip check
- [ ] **12.379. 凭据是否临时隔离** — 是否使用随机临时凭据和隔离数据目录，而非 `.env.example` 占位值
- [ ] **12.380. 验收是否覆盖安全头** — `/login` 200 后是否检查 nosniff / Referrer-Policy / frame deny / Permissions-Policy
- [ ] **12.381. 失败日志是否保留** — 失败时是否输出容器日志
- [ ] **12.382. 清理是否始终执行** — `always` 是否 down 容器并删除临时数据
- [ ] **12.383. 范围是否保持** — 是否未改业务代码、依赖、数据库、Schema 或版本

## Phase 2-L5 CI 最小权限与重复运行控制检查

- [ ] **12.384. 权限是否最小只读** — workflow 是否只声明 `contents: read`，未请求写权限或额外密钥
- [ ] **12.385. 重复运行是否取消** — 是否按 workflow / ref 分组并启用 `cancel-in-progress`
- [ ] **12.386. 原验收是否保持** — test、pip check、Docker smoke、失败日志和清理步骤是否不变

## Phase 2-L6 Docker 健康状态与就绪验收检查

- [ ] **12.387. 镜像健康检查是否最小** — 是否只用 Python 标准库访问现有 `/login`，未安装 curl、依赖或新增接口
- [ ] **12.388. CI 是否先等 healthy** — 是否在原 HTTP / 安全头检查前等待容器健康状态为 `healthy`
- [ ] **12.389. 失败与清理是否保持** — 失败日志和 `always()` 清理是否继续执行
- [ ] **12.390. WSL 验收是否完整** — pytest、pip check、隔离 Docker healthy、版本和清理是否通过

## Phase 2-L7 Docker 运行时安全基线检查

- [ ] **12.391. 根文件系统是否只读** — 生产与 CI Compose 是否启用 `read_only: true`
- [ ] **12.392. capabilities 是否清空** — 是否配置 `cap_drop: ALL`，且容器实际 `CapEff` 为零
- [ ] **12.393. 提权是否关闭** — 是否配置 `no-new-privileges`，且容器实际 `NoNewPrivs` 为 1
- [ ] **12.394. 写入边界是否最小** — `/tmp` 是否为 64 MiB tmpfs，且仅 `/tmp` 与持久化 `/app/data` 可写
- [ ] **12.395. 镜像路径是否只读** — `/app`、`/etc`、`/usr/local` 的实际写入是否失败
- [ ] **12.396. 数据准备是否安全** — README 是否要求 rootful Docker 启动前准备数据目录权限，并提醒存量安装先停机和完成可验证备份
- [ ] **12.397. CI 与清理是否保持** — CI 是否使用相同安全配置，同时保留 test、pip check、Docker smoke、失败日志与清理流程

## Phase 2-L8 固定非 root 容器用户检查

- [ ] **12.398. 固定身份是否存在** — 镜像是否创建 `nsfwtrack` UID/GID `10001:10001`，且 Dockerfile `USER` / `Config.User` 均为 `10001:10001`
- [ ] **12.399. 实际进程身份是否正确** — 应用容器内 `id -u` 与 `id -g` 是否均为 `10001`，健康检查是否继承同一镜像用户
- [ ] **12.400. CI bind mount 是否匹配** — 隔离数据目录是否在启动前以 0700 归 `10001:10001` 所有
- [ ] **12.401. L7 安全边界是否保持** — ReadonlyRootfs、`cap_drop: ALL`、`CapEff=0`、`NoNewPrivs=1` 和 `/tmp` tmpfs 是否继续成立
- [ ] **12.402. 写入边界是否保持** — `/app/data` 与 `/tmp` 是否可写，`/app`、`/etc`、`/usr/local` 是否不可写
- [ ] **12.403. SQLite 是否真实持久化** — 初次启动是否创建 Schema 1 数据库，停止并重建容器后同一 bind mount 是否仍为 Schema 1
- [ ] **12.404. HTTP 验收是否保持** — 初次与重建后的容器是否 healthy，`/login` 200 与安全响应头是否正常
- [ ] **12.405. 升级文档是否安全** — 首次安装和 v1.0.3 存量流程是否要求停机、可验证备份、`10001:10001` 和 0700，且不使用 777、root 入口、sudo/gosu 或自动 `chown`
- [ ] **12.406. 范围与清理是否保持** — 是否未改业务代码、依赖、数据库、Schema、迁移、版本或旧 tag / Release，未部署 N100，且临时资源已清理

## Phase 3-A1 来源链接与批量书签导入检查

- [ ] **12.407. 规则授权是否准确** — RULE 是否只开放保存用户 URL、解析本地书签 HTML / 纯文本清单，并继续禁止外部请求、爬虫、远程图片、adapter、同步、推荐和 AI
- [ ] **12.408. 来源模型是否完整** — `item_sources` 是否包含 item 外键、原 URL、全局唯一规范化 URL、可选标题、创建时间和级联删除
- [ ] **12.409. 规范化是否确定** — 是否仅接受无凭据 HTTP/HTTPS，统一 scheme/IDNA host/default port/percent escape/root path 并移除 fragment
- [ ] **12.410. 多来源与唯一性是否兼顾** — 一个条目是否可有多个不同来源，同规范化 URL 是否不能跨条目重复
- [ ] **12.411. 详情 CRUD 是否安全** — 是否登录保护、添加 POST、删除 POST 且双端确认，删除来源不删除条目
- [ ] **12.412. 本地输入是否完整** — 是否支持一行一 URL、`标题<TAB>URL` 和用户上传浏览器书签 HTML
- [ ] **12.413. 预览统计是否真实** — dry-run 是否零写入并分别统计新增、重复、无效、冲突和新条目
- [ ] **12.414. 写入是否事务化** — 确认写入是否重新校验并单事务提交，故障是否回滚所有新条目和来源
- [ ] **12.415. 无标题是否安全** — 是否只从规范化 URL 本地生成可读占位标题，不获取远程标题 / 元数据
- [ ] **12.416. Schema 迁移是否真实** — CURRENT_SCHEMA_VERSION 是否为 2，生产 registry 是否仅含真实 `create_item_sources` 1 → 2 步骤
- [ ] **12.417. 迁移是否保留旧数据** — 预览是否只读，apply 是否只创建来源表并登记版本 2，旧条目和既有表是否保持
- [ ] **12.418. 备份导入是否兼容** — JSON 备份 / 恢复和 CSV/JSON 条目导入导出是否含来源，旧文件缺少来源是否仍兼容
- [ ] **12.419. i18n 与页面是否完整** — 中英文 key 是否对称，导航、详情、预览、统计、错误和确认文案是否齐全
- [ ] **12.420. 禁止项是否保持** — 是否无外部 HTTP、爬虫、adapter、远程图片、自动同步、推荐、AI、新依赖、v1.0.4 tag/Release 变化或 N100 部署
- [ ] **12.421. 验收是否完整** — 是否通过全量 pytest、pip check、隔离 Schema 1 → 2 / 旧数据、Docker、Actions 并清理
- [ ] **12.422. 同名目标歧义是否安全** — 标题 casefold 后匹配多个已有条目时，预览与 apply 是否均明确冲突且不写入；混合批次正常记录和同批新标题多 URL 行为是否保持

## Phase 3-A2 本地媒体库检查

- [ ] **12.423. 扫描边界是否安全** — 是否仅递归扫描 `data/media` 普通文件，不跟随文件或目录符号链接，不允许路径越出媒体根目录
- [ ] **12.424. 上传限制是否完整** — 是否支持单图 / 多图并在写入前限制单文件大小和批次数量
- [ ] **12.425. 图片真实性是否校验** — 扩展名、MIME 与文件结构是否一致；是否只允许 AVIF / GIF / JPEG / PNG / WebP 并拒绝 SVG、HTML、伪装和损坏文件
- [ ] **12.426. 去重是否确定** — 是否按完整文件 SHA-256 内容寻址，既有文件和同批重复内容均不重复保存
- [ ] **12.427. 引用状态是否真实** — 媒体库是否展示条目封面和创作者头像引用，未引用文件是否明确显示
- [ ] **12.428. 关联操作是否安全** — 是否可设置 / 替换 / 清除封面与头像，全部写操作登录保护且仅 POST，清除是否要求服务端确认并在 strict 模式要求 `CONFIRM`，且不删除媒体文件
- [ ] **12.429. 损坏降级是否安全** — 缺失、损坏、过大、符号链接或非法媒体是否不渲染外链、文件端点 404、页面不 500
- [ ] **12.430. 数据与网络边界是否保持** — 是否无新表、Schema / 迁移 / 依赖 / 版本变化，无外部 URL 请求、远程图片、识别、推荐或 AI
- [ ] **12.431. Docker 边界是否保持** — fixed `10001:10001`、只读根、零 capability、no-new-privileges、`/tmp` tmpfs 和 `/app/data` 持久化是否不变
- [ ] **12.432. 验收是否完整** — 中英文和文档是否同步，全量 pytest、pip check、Docker 双生命周期、Actions 是否通过，临时资源是否清理
- [ ] **12.433. 上传落盘是否原子且可回滚** — 是否先写 `data/media/library` 内随机临时文件，完成 flush / fsync 后以无覆盖原子操作发布；目标已存在时是否复核内容；任意单文件或批次失败是否清除临时文件和本批次新文件

## Phase 3-A3 本地媒体候选配对检查

- [ ] **12.434. 候选生成是否只读** — GET / 扫描是否只基于本地状态生成候选，不自动写入封面或头像
- [ ] **12.435. 匹配规则是否确定可解释** — 是否先做 NFKC / casefold 精确匹配，再做仅保留字母数字的规范化匹配，并展示依据与置信等级
- [ ] **12.436. 后缀范围是否准确** — 分隔的 `cover` 是否只指向条目、`avatar` 是否只指向创作者，未约定后缀是否仍按明确名称规则匹配两类目标
- [ ] **12.437. 双向歧义是否阻断** — 一媒体多目标和一目标多媒体是否均标记冲突，UI 不可选择且伪造 POST 也被拒绝
- [ ] **12.438. 目标与媒体范围是否最小** — 是否只处理有效未使用媒体、无封面条目和无头像创作者
- [ ] **12.439. 单项确认是否安全** — 是否登录保护、仅 POST、浏览器和服务端确认，strict 是否精确验证 `CONFIRM`
- [ ] **12.440. 当前页批量边界是否真实** — 是否只处理当前 20 行候选页中手动选择项，拒绝缺失页码、跨页 ID 和超过页容量的请求
- [ ] **12.441. 写入是否二次验证并事务化** — 提交时是否重新生成候选、复核文件和目标，陈旧 / 冲突 / 不可用时零写入，合法批次一次提交
- [ ] **12.442. 现有关联是否不覆盖** — 生成和提交阶段是否都排除已有封面或头像，竞态 / 陈旧候选是否不会覆盖现有路径
- [ ] **12.443. 物理文件是否保持** — 候选与确认是否不下载、创建、识别、移动、重命名、覆盖或删除媒体文件
- [ ] **12.444. 范围是否保持** — 是否无新表、Schema / 迁移 / 依赖 / 版本 / Docker 配置变化，无外部网络、AI、图像识别、推荐、旧 tag / Release 修改或 N100 部署
- [ ] **12.445. 验收是否完整** — 中英文、文档、专项 / 全量 pytest、pip check、Docker 和 Actions 是否通过，资源是否清理

## Phase 3-A4 未匹配媒体快速建档检查

- [ ] **12.446. 候选来源是否准确** — 是否只使用有效、未引用且没有 A3 配对的本地图片，损坏、已使用和已有配对媒体是否排除
- [ ] **12.447. 文件名规则是否准确** — 是否移除图片扩展名与封面约定后缀，头像约定文件是否完全排除
- [ ] **12.448. 预览是否只读可编辑** — GET 是否零写入，默认标题是否可在确认前修改且限制 1–255 字符
- [ ] **12.449. 默认冲突是否可解释** — 是否展示无效标题、已有精确同名、已有规范化同名和候选间规范化同名，并允许编辑解决
- [ ] **12.450. 最终标题是否重新校验** — POST 是否只以最终输入判断，并拒绝已有精确 / 规范化同名及所选批次内规范化同名
- [ ] **12.451. 单项确认是否安全** — 是否登录保护、仅 POST、浏览器与服务端确认，strict 是否精确验证 `CONFIRM`
- [ ] **12.452. 当前页批量边界是否真实** — 是否只处理当前 20 行候选页中手动选择项，并拒绝缺页、跨页、重复 ID、标题数量错位或超过容量请求
- [ ] **12.453. 提交是否重新扫描** — 是否重新生成候选并复核媒体仍有效、未引用、未匹配且非头像文件，陈旧或伪造 ID 是否拒绝
- [ ] **12.454. 事务是否完整** — 是否在所有校验通过后一次创建条目并设置 `cover_path`，任一文件、insert、flush 或 commit 失败是否整批 rollback
- [ ] **12.455. 媒体是否保持不变** — 成功和失败路径是否都不创建、下载、识别、移动、重命名、覆盖或删除媒体文件
- [ ] **12.456. 范围是否保持** — 是否无新表、Schema / 迁移 / 依赖 / 版本 / Docker 配置变化，无网络、AI、图像识别、旧 tag / Release 修改或 N100 部署
- [ ] **12.457. 验收是否完整** — 中英文、文档、专项 / 全量 pytest、pip check、Docker 与 Actions 是否通过，临时资源是否清理

## Phase 3-A5 媒体库检索与分页检查

- [ ] **12.458. 搜索是否覆盖文件名与路径** — `media_q` 是否对相对文件名 / 路径和 `/media/...` 路径做 NFKC 大小写无关包含匹配
- [ ] **12.459. 搜索边界是否安全** — 空白和超过 200 字符输入是否安全回退，不请求网络、不解释路径且不 500
- [ ] **12.460. 可用 / 损坏筛选是否准确** — 是否严格根据扫描条目的 `available` 状态划分，不隐藏损坏记录或尝试读取外部资源
- [ ] **12.461. 已使用 / 未使用是否准确** — 是否根据条目封面与创作者头像当前引用划分，并同时覆盖可用和损坏条目
- [ ] **12.462. 排序是否完整稳定** — 是否支持文件名升降序、大小升降序，并以规范化文件名 / 路径稳定处理并列
- [ ] **12.463. 媒体分页是否固定 20** — `media_page` 是否每页最多 20 条，非法、负数、非数字和越界值是否回退或夹取
- [ ] **12.464. 三套分页状态是否保留** — media / match / create 任一分页链接是否保留另两套当前页及规范化筛选参数
- [ ] **12.465. 表单返回状态是否保留** — 上传、手动关联、A3 配对和 A4 建档表单及重定向是否保留媒体查询与三套页码
- [ ] **12.466. 空结果是否清楚** — 空媒体库与筛选无结果是否有不同的中英文状态和正确的筛选结果计数
- [ ] **12.467. GET 是否零写入** — 搜索、筛选、排序和翻页前后数据库、关联与媒体文件字节是否完全不变
- [ ] **12.468. A3/A4 是否不变** — 两类候选是否继续基于完整原始扫描，不受媒体卡片搜索 / 筛选 / 排序 / 分页影响
- [ ] **12.469. 范围是否保持** — 是否无媒体操作、网络、AI、新表、Schema / 迁移 / 依赖 / 版本 / Docker 配置、旧 tag / Release 或 N100 部署变化
- [ ] **12.470. 验收是否完整** — i18n、文档、专项 / 全量 pytest、pip check、Docker 和 Actions 是否通过，临时资源是否清理

## Phase 3-A6 本地媒体完整性审计检查

- [ ] **12.471. media 分类是否完整** — `/data-health` 是否增加媒体分类、分类计数、问题 / 警告计数和统一明细
- [ ] **12.472. 引用路径是否安全审计** — 条目封面与创作者头像是否覆盖非法本地值、外部 URL、路径越界、符号链接、缺失和损坏
- [ ] **12.473. 媒体根异常是否安全** — 缺失、符号链接、非目录、不可读或扫描失败是否明确报告且不导致页面 500
- [ ] **12.474. 缺失根语义是否准确** — 无有效本地引用且目录尚未初始化时是否保持健康，存在有效引用时是否同时报告根目录与断裂引用
- [ ] **12.475. 上传残留是否只读报告** — `.upload-*.tmp` 是否作为 warning 报告且不自动删除、移动或修改
- [ ] **12.476. 重复内容是否按 SHA-256 报告** — 不同路径下相同有效图片是否形成 warning，正常唯一未使用媒体是否不算问题
- [ ] **12.477. 跳过项是否汇总** — 安全扫描跳过的符号链接和不支持文件是否分别汇总且不跟随或读取外部目标
- [ ] **12.478. 明细限制是否沿用** — 所有分类是否继续共享 200 条明细上限，同时完整总数与分类计数不被截断
- [ ] **12.479. GET 是否零写入** — 生成报告和渲染页面前后 SQLite、封面 / 头像引用及媒体文件字节是否不变
- [ ] **12.480. 媒体修复是否明确禁止** — media issue code 是否不进入修复白名单，页面无媒体修复表单且伪造 POST 被拒绝
- [ ] **12.481. 既有阶段是否保持** — A3 / A4 / A5 服务与行为是否不变，无网络、AI、媒体物理操作、新表、Schema / 迁移 / 依赖 / 版本变化
- [ ] **12.482. 验收是否完整** — 中英文、文档、专项 / 全量 pytest、pip check、Docker 和 Actions 是否通过，临时资源是否清理且未创建 Release

## Phase 3-B1 重复媒体定位检查

- [ ] **12.483. 分组依据是否严格** — 是否只按可用媒体完整合法 SHA-256 分组，且同组至少有两个不同路径
- [ ] **12.484. 排除规则是否准确** — 损坏媒体、空 / 非法 SHA-256 和只出现一次的内容是否均不标记为重复
- [ ] **12.485. 汇总是否完整** — 是否展示重复组数、涉及文件数及每组保留一份时可节省的总字节数
- [ ] **12.486. duplicate 筛选是否准确** — `media_status=duplicate` 是否只返回重复组成员，并继续兼容现有搜索、排序和分页
- [ ] **12.487. SHA 搜索是否准确** — `media_q` 是否保留文件名 / 路径包含搜索并支持大小写无关的完整 SHA-256 或前缀搜索
- [ ] **12.488. 卡片信息是否完整稳定** — 每个重复成员是否显示组内数量与稳定排序的其他媒体路径
- [ ] **12.489. 状态是否完整保留** — `media_page`、`match_page`、`create_page` 及搜索 / 状态 / 排序是否在三套分页和既有表单间保留
- [ ] **12.490. GET 是否零写入** — 浏览前后 SQLite、封面 / 头像引用及全部媒体文件路径和字节是否完全不变
- [ ] **12.491. A3/A4 是否保持** — 两类候选是否仍使用完整原始扫描，候选 ID 与 B1 搜索 / 筛选 / 分页无关
- [ ] **12.492. 中英文与移动端是否可读** — 新状态、统计、搜索提示、组数量和其他路径是否双语对称且长 SHA / 路径可换行
- [ ] **12.493. 范围是否保持** — 是否无媒体写操作、网络、AI、Schema 2 / 迁移 / 依赖 / 版本 / Docker / tag / Release 或 N100 变化
- [ ] **12.494. 验收是否完整** — 专项 / 全量 pytest、pip check、Docker、Actions、文档和临时资源清理是否全部有证据

## Phase 3-B2 重复媒体组视图检查

- [ ] **12.495. 页面是否只读且登录保护** — `/media-library/duplicates` 是否未登录跳转，浏览与 keeper 选择 GET 是否零写入，且 B3 执行使用独立确认 POST
- [ ] **12.496. B1 边界是否真正复用** — B1 文件列表和 B2 组视图是否调用同一完整合法 SHA-256 / 不同路径分组服务
- [ ] **12.497. 分组是否唯一稳定** — 一个真实 SHA 是否只显示一次，成员是否按规范化路径稳定排序
- [ ] **12.498. 组指标是否准确** — 是否展示成员数、单文件大小、总占用、每组保留一份时可节省空间和完整 SHA-256
- [ ] **12.499. 成员引用是否准确** — 每个成员是否展示路径、可用状态、条目封面与创作者头像引用，未引用时是否明确显示
- [ ] **12.500. 搜索是否完整** — 是否支持 NFKC 大小写无关的文件名 / 路径包含搜索和完整 SHA / 前缀搜索
- [ ] **12.501. 排序是否完整稳定** — 成员数、可节省空间和 SHA 是否均支持升降序，并使用 SHA 稳定处理数值并列
- [ ] **12.502. 分页与回退是否安全** — 是否每页最多 20 组，查询状态是否保留，非法超长搜索、排序和页码是否安全回退或夹取
- [ ] **12.503. 精确链接是否完整** — 是否使用完整 SHA 和 `media_status=duplicate` 跳转 `/media-library#media-files`
- [ ] **12.504. GET 是否零写入** — 浏览前后 SQLite、封面 / 头像引用、媒体路径和字节是否完全不变
- [ ] **12.505. B1/A3/A4 是否保持** — B1 统计 / 筛选与 A3/A4 候选 ID 是否不受 B2 查询影响
- [ ] **12.506. 范围是否保持** — 是否无保留建议、引用迁移、媒体操作、网络、AI、Schema 2 / 迁移 / 依赖 / 版本 / Docker / tag / Release 变化
- [ ] **12.507. 验收是否完整** — 中英文、长 SHA / 路径响应式、专项 / 全量 pytest、pip check、Docker、Actions 和清理是否均有证据

## Phase 3-B3 重复媒体手动整理检查

- [ ] **12.508. keeper 是否必须明确选择** — 重复组是否无预选、推荐、评分或默认 keeper，缺少选择是否服务端拒绝
- [ ] **12.509. 预览是否登录且零写入** — GET 是否只列出 keeper、引用迁移、待删除路径和预计空间，前后 SQLite、媒体字节与 A3/A4 候选是否不变
- [ ] **12.510. B1/B2 边界是否复用** — 是否只处理共享分组服务确认的单个完整合法 SHA-256 组，不接受任意路径列表
- [ ] **12.511. 快照是否防陈旧** — POST 是否比较预览成员与重新扫描成员，组扩张、缩减或重复 / 伪造成员是否全部拒绝
- [ ] **12.512. 危险确认是否统一** — 执行是否要求登录、POST、浏览器确认、服务端确认，并在 strict 模式精确验证 `CONFIRM`
- [ ] **12.513. 文件异常是否拒绝** — 哈希变化、缺失、损坏、符号链接、路径越界、非组成员和同内容 inode 替换是否在删除前拒绝
- [ ] **12.514. 引用是否始终安全** — 删除前是否创建已验证同文件系统锚点，并让所有组内封面 / 头像引用在完整删除窗口指向存在、合法、完整 SHA-256 正确的文件
- [ ] **12.515. keeper 竞态是否关闭** — keeper 是否从不进入删除列表，且在首删前、删除一半后或末次删除期间缺失 / 替换都不会留下断裂引用或丢失最后有效副本
- [ ] **12.516. 删除是否身份受限** — 每个删除是否复核路径、完整 SHA、device、inode、size、mtime 与 ctime，且不处理其他组
- [ ] **12.517. 删除失败是否安全可重试** — 失败路径是否保留、引用是否已安全指向有效 keeper / 恢复路径、原因是否逐项展示并可重新预览重试
- [ ] **12.518. 半成功摘要是否准确** — 是否分别展示迁移数、成功删除路径、实际释放字节、失败和目录同步警告，不泄露异常堆栈
- [ ] **12.519. A3/A4 与本地边界是否保持** — 是否未改候选算法、未请求外部网络、未使用 AI / 图像识别且未自动批量整理
- [ ] **12.520. 中英文与响应式是否完整** — keeper 选择、预览、危险提示、结果、失败原因和重试入口是否双语对称且长路径可读
- [ ] **12.521. 技术边界是否保持** — 是否保持版本 1.0.6、Schema 2、迁移、依赖、Docker/CI 配置和既有 tag / Release
- [ ] **12.522. 验收是否完整** — 陈旧 / 伪造 / 文件异常、迁移顺序、提交失败、删除失败重试、全量 pytest、pip check、Docker 与 Actions 是否均有证据
- [ ] **12.523. 恢复是否禁止覆盖** — keeper 缺失时是否仅无覆盖恢复，路径已被外部文件占用时是否保留该文件并把引用迁移到唯一验证恢复路径
- [ ] **12.524. 锚点生命周期是否完整** — 锚点是否独占创建、复制前后校验身份与哈希、文件和目录 fsync，并在成功、异常及删除失败重试后无临时文件残留
- [ ] **12.525. 失败报告是否安全明确** — 发布 / 最终提交 / 锚点清理失败时是否明确报告实际安全路径，并始终保留至少一份有效同哈希内容

## Phase 3-B4 媒体清理恢复中心检查

- [ ] **12.526. 页面是否登录保护且查询只读** — `/media-library/recovery` 是否未登录跳转，B4 列表查询 GET 是否零写入，且 B5 恢复只通过独立预览与确认 POST 执行
- [ ] **12.527. 前缀识别是否精确** — 是否仅按大小写敏感 basename 的 `.cleanup-anchor-` / `recovered-` 开头识别，不使用 contains，不误判目录名或中间包含文本
- [ ] **12.528. 锚点是否从普通媒体隔离** — 合法、损坏、错误扩展和符号链接锚点是否均不进入普通媒体库与普通扫描统计
- [ ] **12.529. B1/B2 是否隔离锚点** — 锚点是否不参与完整 SHA-256 重复统计、筛选或重复组成员，并保持原分组语义
- [ ] **12.530. 上传去重是否隔离锚点** — 仅有同哈希锚点时，上传是否创建正常媒体而不返回内部锚点路径
- [ ] **12.531. A3/A4 是否隔离锚点** — 锚点是否不进入配对 / 建档候选，且添加锚点前后所有非锚点候选 ID 完全一致
- [ ] **12.532. recovered 是否保持正常媒体** — 恢复文件是否仍参与普通媒体、重复组和候选流程，并有独立筛选与清晰标识
- [ ] **12.533. 恢复中心字段是否完整** — 每项是否展示完整路径、实际大小、完整 SHA（可校验时）、合法性及封面 / 头像引用
- [ ] **12.534. 恢复状态是否准确** — 是否区分已引用锚点、未引用锚点、损坏锚点和正常恢复文件，损坏锚点仍展示引用
- [ ] **12.535. 搜索排序分页是否稳定** — 是否支持路径 / SHA 搜索、路径 / 大小 / SHA / 状态稳定双向排序及固定 20 条分页
- [ ] **12.536. data-health 是否覆盖锚点** — 是否分别报告已引用、未引用和损坏锚点，并保留普通媒体引用异常且不为锚点提供 fix
- [ ] **12.537. GET 是否零写入** — 普通媒体、恢复中心、B1/B2 和 data-health 浏览前后数据库引用、媒体路径及字节是否不变
- [ ] **12.538. 中英文和长路径是否可读** — 页面、筛选、状态、合法性、引用和 data-health 文案是否中英文对称且完整 SHA / 路径可换行
- [ ] **12.539. 技术边界是否保持** — 是否保持版本 1.0.6、Schema 2、迁移、依赖、Docker/CI、旧 tag / Release 和 N100 状态
- [ ] **12.540. 验收是否完整** — B4 专项、媒体链回归、全量 pytest、pip check、Docker、Actions、文档及临时资源清理是否全部有证据

## Phase 3-B5 安全锚点手动恢复检查

- [ ] **12.541. 是否仅操作单个合法锚点** — 是否只能从恢复中心明确选择一个 available cleanup anchor，损坏、符号链接、错误扩展、普通文件和 `recovered-*` 是否无执行入口且服务端拒绝
- [ ] **12.542. 预览是否登录且零写入** — GET 是否展示完整路径 / SHA / MIME / size / device / inode / mtime / ctime、全部引用和后果，且数据库引用、媒体路径与字节不变
- [ ] **12.543. 危险确认是否复用** — POST 是否要求登录、浏览器确认、服务端 `confirm=1`，strict 模式是否精确验证 `CONFIRM`
- [ ] **12.544. 完整身份是否重验** — POST 是否重新扫描并逐项比对路径、类型、完整 SHA、device、inode、size、mtime 和 ctime，拒绝陈旧、伪造或变化请求
- [ ] **12.545. 发布是否无覆盖且唯一** — 是否复用安全发布能力，在同目录独占创建唯一 `recovered-*`，目标碰撞时不覆盖并重新选名
- [ ] **12.546. 持久化是否完整** — 发布后是否验证锚点 / 恢复文件为合法同 SHA 同 inode，并完成文件与目录 fsync
- [ ] **12.547. 引用迁移是否完整事务化** — 是否在一个事务中迁移全部条目封面与创作者头像引用，并在事务内复核原锚点零引用
- [ ] **12.548. 删除顺序是否安全** — 是否先提交引用到恢复文件，再锁定复核恢复身份与原锚点零引用，最后按完整身份删除锚点
- [ ] **12.549. DB 失败补偿是否完整** — 任一更新、复核或 commit 失败时是否回滚全部引用、保留原锚点并身份清理本次新恢复文件
- [ ] **12.550. 删除失败是否安全报告** — unlink / fsync / 身份 / 引用复核失败时是否保持引用在恢复文件，不删除内容，并报告锚点残留或持久化警告
- [ ] **12.551. 已引用与未引用是否均支持** — 两类合法锚点是否都能恢复，迁移计数是否准确，成功后恢复文件存在、合法且完整 SHA 一致
- [ ] **12.552. 普通入口是否隔离锚点** — 条目 / 创作者创建、编辑和媒体库封面 / 头像设置是否不能新建 cleanup anchor 引用，同时不破坏已有内部引用编辑兼容性
- [ ] **12.553. B3 / 备份是否保持** — B3 内部锚点流程、既有备份恢复兼容性和历史锚点引用是否未被通用路径校验误伤
- [ ] **12.554. B1/B2/A3/A4 是否保持** — 普通扫描、重复分组、上传去重、配对和建档候选是否仍遵守 B4 隔离，恢复文件仍是普通媒体
- [ ] **12.555. 范围是否保持** — 是否无批量恢复、直接丢弃、`recovered-*` 操作、自动修复、网络、AI、Schema / 迁移 / 依赖 / 版本 / Docker/CI 变化
- [ ] **12.556. 中英文与长身份是否可读** — 预览、后果、确认、成功、拒绝、回滚和残留文案是否双语对称，长路径 / SHA / inode / 时间戳是否可读
- [ ] **12.557. 验收是否完整** — 专项故障注入、媒体链、全量 pytest、pip check、隔离 Docker、Actions、文档、受保护状态与临时资源清理是否均有证据

## Phase 3-B6 无引用安全锚点手动清理检查

- [ ] **12.558. 是否仅暴露零引用单项入口** — 是否只有合法 `anchor_unreferenced` 行提供单项永久删除预览，已引用 / 损坏 / recovered 行是否没有可执行入口
- [ ] **12.559. GET 是否登录且零写入** — 预览是否展示完整路径 / SHA / MIME / size / device / inode / mtime / ctime 和永久后果，且数据库与全部文件不变
- [ ] **12.560. 危险确认是否完整** — POST 是否要求登录、浏览器确认、服务端 `confirm=1`，strict 模式是否精确验证 `CONFIRM`
- [ ] **12.561. 提交是否重新扫描** — 是否重新定位合法锚点并逐项比对完整 SHA、device、inode、size、mtime 和 ctime，拒绝陈旧、伪造、缺失或变化请求
- [ ] **12.562. 写锁是否在引用复核前获取** — 是否结束预检读事务后执行 SQLite `BEGIN IMMEDIATE`，并在持锁状态重新查询条目封面和创作者头像引用
- [ ] **12.563. 引用竞态是否安全拒绝** — 预览后新增任一引用时是否在 unlink 前拒绝，目标文件保留且竞态引用不被迁移或清除
- [ ] **12.564. 最终身份与 fsync 是否完整** — 零引用后是否再次验证完整身份与 SHA，身份绑定 unlink 后是否 fsync 所在目录
- [ ] **12.565. 删除范围是否精确** — 是否只删除用户确认的一个路径，不触碰其他锚点、普通媒体或 `recovered-*`，且不创建安全副本
- [ ] **12.566. 非法目标是否全面拒绝** — 已引用、损坏、符号链接、错误扩展、普通、recovered、缺失、陈旧、伪造和同路径替换是否全部拒绝
- [ ] **12.567. 删除失败是否零 DB 修改** — 身份 / unlink / 写锁失败是否保留文件，不迁移、清除或更新任何引用，并展示明确原因
- [ ] **12.568. 持久化警告是否准确** — unlink 成功但目录 fsync 或锁释放失败时，是否报告文件已移除与实际 warning，而不误称文件仍在
- [ ] **12.569. 状态页是否自然收敛** — 成功删除后恢复中心和 Data Health 是否不再报告目标，且没有新增自动 fix
- [ ] **12.570. B3-B5 是否保持** — B3 安全锚点、B4 分类审计、B5 恢复发布与普通交互入口隔离是否均无回归
- [ ] **12.571. B1/B2/A3/A4 与备份是否保持** — 普通扫描、重复组、候选、上传去重和备份兼容是否未变化
- [ ] **12.572. 范围是否保持** — 是否无批量 / 自动清理、引用修改、网络、AI、版本 / Schema / 迁移 / 依赖 / Docker/CI / tag / Release / N100 变化
- [ ] **12.573. 双语与验收是否完整** — 中英文、长身份字段、专项 / 全量 pytest、pip check、Docker、Actions、文档和资源清理是否均有证据

## Phase 3-C1 断裂媒体引用手动修复检查

- [ ] **12.574. 单项入口范围是否准确** — 是否只为 item cover / creator avatar 的 missing、damaged、symlink、invalid / escaping path 和损坏锚点引用提供入口，健康引用与其他问题无入口
- [ ] **12.575. GET 是否登录且零写入** — 预览是否展示对象、原路径、问题类型、对象快照和后果，且数据库与全部媒体路径 / 字节不变
- [ ] **12.576. 用户选择是否完全显式** — 是否只能手动选择一个现有合法媒体替换或明确清除，不存在默认候选、自动推荐、自动清除或批量修复
- [ ] **12.577. 候选搜索分页是否稳定** — 是否支持路径 / SHA 搜索、规范化路径稳定排序、固定 20 条分页及查询状态保留
- [ ] **12.578. 候选合法性是否严格** — 是否只列出并接受存在、普通、合法、完整验证通过的本地媒体，`recovered-*` 可用而 cleanup anchor / damaged / symlink 始终排除
- [ ] **12.579. 危险确认是否统一** — 替换与清除 POST 是否都要求登录、浏览器确认、服务端 `confirm=1`，strict 模式是否精确验证 `CONFIRM`
- [ ] **12.580. 对象与原引用是否重验** — POST 是否重读对象完整快照并精确比较原 `cover_path` / `avatar_path`，对象删除、变化或引用变化是否拒绝
- [ ] **12.581. 问题状态是否重验** — 提交是否重新执行当前媒体健康分类，问题消失或类型变化是否拒绝
- [ ] **12.582. 替代身份是否完整重验** — 是否核对路径、完整 SHA、size、device、inode、mtime、ctime，并在条件更新后提交前再次验证同一身份
- [ ] **12.583. 条件更新是否精确** — 是否在 `BEGIN IMMEDIATE` 内仅更新用户确认对象的一个字段，并同时以 id 和原引用作为 UPDATE 条件
- [ ] **12.584. 清除范围是否精确** — clear 是否只将当前一个无效引用设为空，不改变对象其他字段、其他记录或文件
- [ ] **12.585. DB 失败是否完整回滚** — 写锁、UPDATE、最终身份复核或 commit 失败时是否回滚，目标引用保持原值且无半写入
- [ ] **12.586. 文件是否绝对不变** — 成功、拒绝、竞态和 DB 失败路径是否均不删除、写入、移动或重命名任何普通、recovered 或 anchor 文件
- [ ] **12.587. 修复结果是否准确** — 是否展示旧路径、新路径或清除结果，且 Data Health 不再报告已修复目标而非目标问题仍保留
- [ ] **12.588. 兼容与范围是否保持** — B3-B6、媒体库、备份 / 导入是否无回归，且无网络、AI、版本 / Schema / 迁移 / 依赖 / Docker/CI / tag / Release / N100 变化
- [ ] **12.589. 双语与验收是否完整** — 中英文、长路径 / SHA、专项 / 全量 pytest、pip check、隔离 Docker、Actions、文档和资源清理是否均有证据

## Phase 3-C2 上传残留文件手动清理检查

- [ ] **12.590. 单项入口范围是否准确** — 是否只为 Data Health `media_upload_residue` 提供入口，且每次只选择一个目标
- [ ] **12.591. 精确名称与类型是否严格** — 是否只接受 basename 大小写精确匹配 `.upload-*.tmp` 且中段非空的普通非符号链接文件
- [ ] **12.592. GET 是否登录且零写入** — 是否展示路径、size、device、inode、mtime、ctime、引用和后果，且数据库与全部文件不变
- [ ] **12.593. 临时内容是否绝不读取** — 扫描、预览、确认与拒绝路径是否均不读取、解析、恢复或复制临时文件内容
- [ ] **12.594. 已引用目标是否安全引导** — 是否展示精确封面 / 头像引用和 C1 指引，不提供删除表单且不自动修改引用
- [ ] **12.595. 危险确认是否统一** — POST 是否要求登录、浏览器确认、服务端 `confirm=1`，strict 模式是否精确验证 `CONFIRM`
- [ ] **12.596. 身份快照是否完整重验** — 是否提交并重验 path、size、device、inode、mtime、ctime，伪造、变化和同路径替换均拒绝
- [ ] **12.597. 写锁引用复核是否完整** — 是否结束预览读事务后获取 `BEGIN IMMEDIATE`，并在锁内同时确认 item cover 与 creator avatar 引用为零
- [ ] **12.598. 引用竞态是否安全拒绝** — 预览后新增相对或 `/media/` 引用时是否在 unlink 前拒绝，文件与数据库均不变
- [ ] **12.599. 删除是否身份绑定且持久化** — 是否最终重验完整身份、按目录 fd 只 unlink 所选名称，并 fsync 所在目录
- [ ] **12.600. 非法目标是否全面拒绝** — 空中段、近似大小写 / 后缀、目录、符号链接、越界、URL、缺失、陈旧和伪造请求是否全部拒绝
- [ ] **12.601. 未删除失败是否准确** — 锁、引用查询、身份或 unlink 失败时是否保留目标和全部数据库记录，并明确说明未删除
- [ ] **12.602. 已删除同步失败是否准确** — unlink 成功但目录 fsync 失败时是否明确报告文件已经删除及同步警告，不误称文件仍在
- [ ] **12.603. 删除范围是否精确** — 是否不触碰其他残留、普通媒体、cleanup anchor、`recovered-*` 或数据库引用，且不创建恢复副本
- [ ] **12.604. 自动化边界是否保持** — 是否不存在自动、批量、一键清理、定时任务、内容恢复、网络请求、AI 或图像识别
- [ ] **12.605. 回归与版本边界是否完整** — C1、B3-B6、上传、Data Health、备份 / 导入是否通过，且版本 1.0.6、Schema 2、迁移、依赖、Docker/CI、tag / Release 与 N100 状态不变
- [ ] **12.606. 双语与验收证据是否完整** — 中英文、专项 / 全量 pytest、pip check、Docker、Actions、文档、提交推送和资源清理是否均有真实证据

## Phase 3-C3 媒体扫描跳过项定位中心检查

- [ ] **12.607. 逐项模型是否完整兼容** — 是否新增安全相对路径、稳定原因、扩展名与可选 lstat 字段，同时旧四参数 `LocalMediaScan` 构造仍可用
- [ ] **12.608. 五类原因是否精确** — symlink、unsupported extension、special file、directory unreadable、entry error 是否互斥且可解释
- [ ] **12.609. 相对路径是否安全** — 是否只由媒体根下目录 fd 与 entry name 生成，控制字符 / 反斜线安全转义，绝对路径与原始 OSError 不进入结果或页面
- [ ] **12.610. 符号链接是否绝不跟随** — 是否只使用 lstat，子目录通过 `O_DIRECTORY|O_NOFOLLOW` 打开，检查后目录替换竞态也不进入链接目标
- [ ] **12.611. 被跳过内容是否零读取** — unsupported、special、symlink、目录错误和条目错误是否均不打开、读取、解析、验证或哈希内容
- [ ] **12.612. 单项失败是否隔离** — 一个目录 scandir 或条目 lstat 失败时是否只生成一条稳定记录并继续扫描其他兄弟项
- [ ] **12.613. 明细是否确定且去重** — 同一扫描状态多次结果是否一致，并按 casefold path、原 path 和 reason 稳定排序
- [ ] **12.614. 旧汇总是否严格一致** — `skipped_symlinks` 是否等于 symlink 明细数，`skipped_unsupported` 是否等于其他四类明细总数
- [ ] **12.615. 既有扫描边界是否保持** — 普通媒体、invalid image、cleanup anchor、`recovered-*`、上传残留和 include_cleanup_anchors 行为是否不变
- [ ] **12.616. 页面是否登录且只读** — `/media-library/skipped` 是否仅有登录保护 GET，无 POST、删除、移动、改名、恢复、关联或其他写入口
- [ ] **12.617. 页面字段是否完整** — 是否展示安全路径、类型、扩展名及可取得的 size、device、inode、mtime、ctime，并不显示 raw error
- [ ] **12.618. 搜索筛选分页是否稳定** — 路径搜索、五类精确筛选、原 unsupported 聚合筛选、路径 / 类型排序和固定 20 条分页是否保留状态
- [ ] **12.619. Data Health 链接是否对应** — symlink 告警是否链接 symlink 结果，unsupported 告警是否链接四类非 symlink 结果，数量与页面明细一致
- [ ] **12.620. GET 是否零写入** — 跳过项页面和 Data Health GET 前后数据库、所有跳过文件及符号链接是否不变
- [ ] **12.621. 范围是否保持** — 是否无自动操作、定时任务、网络、AI、依赖、数据库、Schema、迁移、Docker/CI、tag / Release 或 N100 变化
- [ ] **12.622. 回归是否完整** — A3-A6、B1-B6、C1-C2、媒体库、上传、Data Health、备份 / 导入是否均通过
- [ ] **12.623. 双语与验收证据是否完整** — 中英文、专项 / 全量 pytest、pip check、Docker、Actions、文档、提交推送和资源清理是否均有真实证据
- [ ] **12.624. 候选身份是否完整保存** — 遍历时是否保存根、每级父目录和最终文件的 dev、inode、size、mtime、ctime，并在读取时逐项精确复核
- [ ] **12.625. 候选读取是否保持在 FD 链内** — 是否从根 fd 逐段以 `O_DIRECTORY|O_NOFOLLOW` 打开父目录，并仅以最终父 fd、文件名和 `O_NOFOLLOW` 打开候选文件
- [ ] **12.626. 后置路径竞态是否安全** — 子目录 fd 已打开后，父路径替换为外部 symlink、最终文件替换或身份变化时，是否在解析 / 哈希前生成安全 `entry_error` 且外部内容零读取
- [ ] **12.627. 读取后是否再次验证** — 内容读取后是否复核全部已开目录 / 文件 fd 和当前根下名称映射，失败时内容不解析、不哈希且不进入 `scan.entries`

## Phase 3-C4 损坏媒体文件手动清理检查

- [ ] **12.628. finding 范围是否精确** — 是否只为带原始 SHA 的允许扩展名损坏普通媒体创建逐项 finding，排除有效媒体、anchor、上传残留、symlink 与所有扫描跳过项
- [ ] **12.629. recovered 是否保持普通媒体** — 损坏 `recovered-*` 是否可进入同一 finding / 预览，而不获得特殊自动处理
- [ ] **12.630. 双入口是否一致** — Data Health finding 与媒体库 invalid 卡片是否只链接同一个登录保护单项预览
- [ ] **12.631. GET 是否完整且零写入** — 是否展示安全路径、原始 SHA、size、device、inode、mtime、ctime、引用和后果，且文件与数据库均不变
- [ ] **12.632. 读取是否复用 C3 FD 链** — 是否从验证根 fd 逐段 `O_DIRECTORY|O_NOFOLLOW` 打开，最终文件只通过父 fd 打开，不通过候选 `Path.stat/read_bytes` 重解析
- [ ] **12.633. 引用目标是否只给 C1 指引** — 当前有封面或头像引用时是否列出对象与 C1 链接且绝不显示删除表单
- [ ] **12.634. 确认策略是否完整** — POST 是否要求登录、浏览器确认、服务端 confirm，并在 strict 模式精确验证 `CONFIRM`
- [ ] **12.635. 快照是否完整重验** — 是否重新确认路径、原始 SHA、size、device、inode、mtime、ctime 及文件仍然损坏，任何变化均拒绝
- [ ] **12.636. 路径竞态是否关闭** — 父目录 / 同路径 / symlink 替换是否在解析、哈希或 unlink 前拒绝，外部同名内容不读取也不删除
- [ ] **12.637. 引用竞态是否锁内拒绝** — 是否结束预览事务、获取 `BEGIN IMMEDIATE`，并在 unlink 前重查 item cover 与 creator avatar 均为零
- [ ] **12.638. 删除是否精确耐久** — 是否只按最终父目录 fd unlink 目标并 fsync 该目录，不触碰其他媒体或创建恢复副本
- [ ] **12.639. 失败状态是否真实** — 锁 / 查询 / 身份 / unlink 失败是否保持目标与数据库不变，fsync 失败是否明确报告文件已删除
- [ ] **12.640. 数据库是否完全不改** — 是否从不迁移、清除或重写引用，也不新增其他数据库写入
- [ ] **12.641. 自动化边界是否保持** — 是否无自动、批量、一键、定时、网络、AI 或图像识别行为
- [ ] **12.642. 回归与版本边界是否完整** — B3-B6、C1-C3、媒体库、上传、恢复、Data Health、备份 / 导入是否通过，且版本 1.0.6、Schema 2、迁移、依赖、Docker/CI、tag / Release 与 N100 不变
- [ ] **12.643. 双语与验收证据是否完整** — 中文 / English、专项 / 全量 pytest、pip check、Docker、Actions、文档、提交推送和资源清理是否均有真实证据

## 登录保护检查

- [ ] **13. 密码是否从环境变量读取** — 不是硬编码
- [ ] **14. 是否禁止空密码**
- [ ] **15. 是否使用 session cookie**
- [ ] **16. `.env` 是否在 `.gitignore` 中**
- [ ] **17. 所有页面和 API 是否默认需登录**
- [ ] **17.1. 语言偏好是否刷新后保留** — `/set-language?lang=zh|en` 是否可用

## 部署检查

- [ ] **18. `docker compose build` 是否通过**
- [ ] **19. `docker compose up` 能否正常启动**
- [ ] **20. `/api/` 端点是否能 curl 通**
- [ ] **21. SQLite 文件是否生成在 `data/` 目录下**
- [ ] **22. Docker 卷挂载是否正确**
- [ ] **22.1. N100 / LAN 部署文档** — README 是否说明端口、`.env`、`./data` 持久化、安全边界和备份建议

## 发布前检查

- [ ] **22.2. README 版本状态** — 是否标明当前发布版本、功能清单、边界和已知限制
- [ ] **22.3. CHANGELOG** — 是否包含当前发布版本、已完成内容、安全说明和已知限制
- [ ] **22.4. 版本号准备** — tag、GitHub Release 与发布文档中的版本号是否一致，且未误改旧 tag
- [ ] **22.5. CI 简洁性** — 是否仍只安装开发依赖并运行完整测试，没有引入复杂发布流程
- [ ] **22.6. 文档 / CI 格式** — Markdown 文件是否为正常多行结构，CI workflow 是否为正常多行 YAML
- [ ] **22.7. v0.2.0 发布准备** — 是否确认 Phase 2-A1 / A2 / A3 / A4 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构或 Phase 3 能力
- [ ] **22.8. v0.3.0 发布准备** — 是否确认 Phase 2-B1 / B2 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构、tag 迁移或 Phase 3 能力
- [ ] **22.9. Phase 2-C1 发布记录** — 是否只记录到 `Unreleased` 或当前 `v0.4.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0`
- [ ] **22.10. Phase 2-C2 发布记录** — 是否只记录到 `Unreleased` 或当前 `v0.4.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0`
- [ ] **22.11. v0.4.0 发布准备** — 是否确认 Phase 2-C1 / C2 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构、旧 tag 迁移或 Phase 3 能力
- [ ] **22.12. Phase 2-D1 发布记录** — 是否只记录到 `Unreleased` 或 `v0.5.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0`
- [ ] **22.13. Phase 2-D2 发布记录** — 是否只记录到 `Unreleased` 或 `v0.5.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0`
- [ ] **22.14. v0.5.0 发布准备** — 是否确认 Phase 2-D1 / D2 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构、旧 tag 迁移或 Phase 3 能力
- [ ] **22.15. Phase 2-E1 发布记录** — 是否只记录到 `Unreleased` 或 `v0.6.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0`
- [ ] **22.16. Phase 2-E2 发布记录** — 是否只记录到 `Unreleased` 或 `v0.6.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0`
- [ ] **22.17. Phase 2-E3 发布记录** — 是否只记录到 `Unreleased` 或 `v0.6.0` 发布段，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0`
- [ ] **22.18. v0.6.0 发布准备** — 是否确认 Phase 2-E1 / E2 / E3 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构、旧 tag 迁移或 Phase 3 能力
- [ ] **22.19. Phase 2-F1 发布记录** — 是否只记录到 `Unreleased`，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0` / `v0.6.0`
- [ ] **22.20. Phase 2-F2 发布记录** — 是否只记录到 `Unreleased`，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0` / `v0.6.0`，且未创建 tag 或 GitHub Release
- [ ] **22.21. Phase 2-F3 发布记录** — 是否只记录到 `Unreleased`，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0` / `v0.6.0`，且未创建 tag 或 GitHub Release
- [ ] **22.22. v0.7.0 发布准备** — 是否确认 Phase 2-F1 / F2 / F3 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构、旧 tag 迁移或 Phase 3 能力
- [ ] **22.23. Phase 2-G1 发布记录** — 是否只记录到 `Unreleased` 或 `v0.8.0`，未误写入 `v0.1.0` / `v0.2.0` / `v0.3.0` / `v0.4.0` / `v0.5.0` / `v0.6.0` / `v0.7.0`
- [ ] **22.24. Phase 2-G6 发布记录** — 是否只记录到 `Unreleased` 或 `v0.8.0`，未误写入 `v0.1.0` 到 `v0.7.0`
- [ ] **22.25. v0.8.0 发布准备** — 是否确认 Phase 2-G1 / G6 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构或 Phase 3 能力，且旧 tag 未移动
- [ ] **22.26. Phase 2-H1 发布记录** — 是否只记录到 `v0.9.0`，未误改 `v0.1.0` 到 `v0.8.0` 的已发布内容或 tag
- [ ] **22.27. Phase 2-H2 发布记录** — 是否只记录到 `v0.9.0`，并保持启动不自动迁移、升级显式触发和升级前备份边界
- [ ] **22.28. v0.9.0 发布准备** — 是否确认 Phase 2-H1 / H2 已在 `main`，只整理 release 文档，未新增业务功能、依赖、数据库结构或 Phase 3 能力
- [ ] **22.29. v0.9.0 迁移真实性** — 是否保持 `CURRENT_SCHEMA_VERSION = 1` 和空生产迁移注册表，未虚构 `1 -> 2` 生产迁移
- [ ] **22.30. 下一阶段状态** — 是否将下一阶段指向 Phase 2-I 稳定性收尾，并将 `Unreleased` 重置为无未发布变更
- [ ] **22.31. Phase 2-I1 记录边界** — 是否只写入 `Unreleased`，未修改 v0.9.0 或更早发布段、旧 tag 或 GitHub Release
- [ ] **22.32. Phase 2-I2 记录边界** — 是否只写入 `Unreleased`，未修改 v0.9.0 或更早发布段、旧 tag 或 GitHub Release
- [ ] **22.33. Phase 2-I3 记录边界** — 是否只写入 `Unreleased`，未修改 v0.9.0 或更早发布段、旧 tag 或 GitHub Release
- [ ] **22.34. Phase 2-I4 记录边界** — 是否只写入 `Unreleased`，未修改 v0.9.0 或更早发布段、旧 tag 或 GitHub Release
- [ ] **22.35. v1.0.0 冻结结论** — 是否完成安全、兼容、数据库、性能和 Docker 总审查，并明确列出仍存在的非阻塞限制
- [ ] **22.36. v1.0.0 版本一致性** — FastAPI 元数据、README、PLAN、TASKS、GOAL、CHANGELOG、tag 和 Release 是否均为 `1.0.0`
- [ ] **22.37. v1.0.0 CHANGELOG** — 是否保留空白 Unreleased，并将 I1-I4 归档为 `## [1.0.0] - 2026-07-11`
- [ ] **22.38. v1.0.0 验收** — 是否运行完整 pytest 和全新隔离 Docker build / up / `/login` / down，且清理临时数据
- [ ] **22.39. v1.0.0 Schema 边界** — 是否保持 Schema 1、空生产迁移注册表、无索引 / 表 / 字段 / 依赖变化
- [ ] **22.40. v1.0.0 引用一致性** — annotated tag 的 peeled commit、`origin/main` 和发布提交是否完全一致
- [ ] **22.41. v1.0.0 正式 Release** — GitHub Release 是否非 draft、非 prerelease，标题和 tag 是否均正确，且未移动旧 tag
- [ ] **22.42. v1.0.1 范围** — 是否只发布 K1 / K2 审计与边界收口，未新增业务功能、依赖、数据库结构、Schema 或迁移
- [ ] **22.43. v1.0.1 版本一致性** — FastAPI 元数据、README、PLAN、TASKS、GOAL、CHANGELOG、tag 和 Release 是否均为 `1.0.1`
- [ ] **22.44. v1.0.1 CHANGELOG** — 是否保留空白 Unreleased，并将 K1 / K2 归档为 `## [1.0.1] - 2026-07-11`
- [ ] **22.45. v1.0.1 验收** — 是否运行 347 项完整 pytest 和全新隔离 Docker build / up / `/login` / version / down，且清理临时数据
- [ ] **22.46. v1.0.1 Schema 边界** — 是否保持 Schema 1、空生产迁移注册表，未接触默认 schema 2 数据卷
- [ ] **22.47. v1.0.1 引用一致性** — annotated tag 的 peeled commit、`origin/main`、Release target 和发布提交是否完全一致
- [ ] **22.48. v1.0.1 正式 Release** — GitHub Release 是否非 draft、非 prerelease，标题和 tag 是否正确，且旧 tag / Release 未移动
- [ ] **22.49. v1.0.2 范围** — 是否只发布 L1 至 L6 维护与 CI 改进，未新增业务逻辑、数据库、Schema 或迁移
- [ ] **22.50. v1.0.2 版本一致性** — FastAPI 元数据、README、PLAN、TASKS、GOAL、CHANGELOG、tag 和 Release 是否均为 `1.0.2`
- [ ] **22.51. v1.0.2 CHANGELOG** — 是否保留空白 Unreleased，并将 L1 至 L6 归档为 `## [1.0.2] - 2026-07-12`
- [ ] **22.52. v1.0.2 验收** — 是否运行 358 项完整 pytest、pip check 和全新隔离 Docker healthy / `/login` / version / down 并清理
- [ ] **22.53. v1.0.2 Schema 边界** — 是否保持 Schema 1、空生产迁移注册表，未新增数据库变化
- [ ] **22.54. v1.0.2 引用一致性** — annotated tag 的 peeled commit、`origin/main`、Release target 和发布提交是否完全一致
- [ ] **22.55. v1.0.2 正式 Release** — GitHub Release 是否非 draft、非 prerelease，标题和 tag 是否正确，且旧 tag / Release 未移动
- [ ] **22.56. v1.0.3 范围** — 是否只发布 L7 Docker 运行时安全增强与权限文档修复，未修改业务逻辑、依赖、数据库、Schema、迁移或容器用户
- [ ] **22.57. v1.0.3 版本一致性** — FastAPI 元数据、README、PLAN、TASKS、GOAL、CHANGELOG、tag 和 Release 是否均为 `1.0.3`
- [ ] **22.58. v1.0.3 CHANGELOG** — 是否保留空白 Unreleased，并将 L7 归档为 `## [1.0.3] - 2026-07-12`
- [ ] **22.59. v1.0.3 验收** — 是否运行 358 项完整 pytest、pip check 和隔离 Docker 安全配置 / healthy / 持久化 / version / down 并清理
- [ ] **22.60. v1.0.3 Schema 与用户边界** — 是否保持 Schema 1、空生产迁移注册表和原容器用户
- [ ] **22.61. v1.0.3 引用一致性** — annotated tag 的 peeled commit、`origin/main`、Release target 和发布提交是否完全一致
- [ ] **22.62. v1.0.3 正式 Release** — GitHub Release 是否非 draft、非 prerelease，标题和 tag 是否正确，且旧 tag / Release 未移动
- [ ] **22.63. v1.0.4 范围** — 是否只发布 L8 固定非 root 身份与数据权限迁移，未修改业务逻辑、依赖、数据库、Schema、迁移、容器 UID/GID 或安全配置
- [ ] **22.64. v1.0.4 版本一致性** — FastAPI 元数据、README、PLAN、TASKS、GOAL、CHANGELOG、tag 和 Release 是否均为 `1.0.4`
- [ ] **22.65. v1.0.4 CHANGELOG** — 是否保留空白 Unreleased，并将 L8 归档为 `## [1.0.4] - 2026-07-12`
- [ ] **22.66. v1.0.4 Docker 验收** — 是否验证 `Config.User` / UID / GID 为 `10001:10001`、L7/L8 安全边界、HTTP / 安全头和 SQLite 重建持久化 / Schema 1
- [ ] **22.67. v1.0.4 升级说明** — Release notes 与 README 是否要求 v1.0.3 及更早版本先停机、完成可验证备份，再将 data 迁移为 `10001:10001` / 0700
- [ ] **22.68. v1.0.4 引用一致性** — annotated tag 的 peeled commit、`origin/main`、Release target 和发布提交是否完全一致
- [ ] **22.69. v1.0.4 正式 Release** — GitHub Release 是否非 draft、非 prerelease，标题和 tag 是否正确，且旧 tag / Release 未移动
- [ ] **22.70. v1.0.5 发布范围是否准确** — 是否仅包含 Phase 3-A1 至 A6、来源同名歧义修复和媒体原子上传修复，未夹带新功能
- [ ] **22.71. v1.0.5 当前状态是否一致** — README、PLAN、TASKS、REVIEW 和 GOAL 是否均标明 v1.0.5 为当前稳定版与最新 Release，A1-A6 已发布
- [ ] **22.72. v1.0.5 CHANGELOG 是否保持** — 是否保留空 Unreleased 和原 `[1.0.5] - 2026-07-13` 发布段，发布后同步未重写 CHANGELOG
- [ ] **22.73. v1.0.5 文档边界是否冻结** — 发布后同步是否只修改文档，保持代码、测试、Schema 2、唯一真实 1 → 2 迁移、依赖和 Docker 配置不变
- [ ] **22.74. v1.0.5 发布引用是否精确** — annotated tag object 是否为 `6a4def572e100198a446ad56353400138c573f66`，peeled commit 是否为 `3c4fee62891ff2826f0b8bc97b33bf3a4d08aa73`
- [ ] **22.75. v1.0.5 Release 是否真实** — `NSFWTrack v1.0.5` 是否为非 Draft、非 Pre-release 的正式 Release，地址是否为 `https://github.com/choneer/nsfwtrack/releases/tag/v1.0.5`
- [ ] **22.76. v1.0.5 发布后同步是否完整** — 是否清除候选 / 待创建描述，提交推送 main、最终 Actions 通过、工作区干净，且未移动 tag 或修改 Release
- [ ] **22.77. v1.0.6 发布范围是否准确** — 正式 Release 是否只包含已冻结的 Phase 3-B1 / B2，未把后续 B3 写入已发布段
- [ ] **22.78. v1.0.6 当前状态是否一致** — README、PLAN、TASKS、REVIEW 和 GOAL 是否均标明 v1.0.6 为当前稳定版与最新 Release，B1/B2 已发布
- [ ] **22.79. v1.0.6 CHANGELOG 是否保持** — `[1.0.6] - 2026-07-13` 是否原样保留，B3 / B4 / B5 是否只写入 Unreleased
- [ ] **22.80. v1.0.6 发布边界是否保持** — B1/B2 发布内容是否仍只读、无媒体操作、无引用迁移、无自动保留建议且 A3/A4 不变
- [ ] **22.81. v1.0.6 技术边界是否保持** — 正式发布是否保持 Schema 2、迁移、依赖和 Docker/CI 安全配置
- [ ] **22.82. v1.0.6 验收是否完整** — 发布前全量 441 passed、pip check、隔离 Docker 双生命周期和两轮 Actions 是否均有记录
- [ ] **22.83. v1.0.6 发布引用是否精确** — tag object 是否为 `d4d5c31cd5b2fed9a90ad69742d54b4c9dbed0b4`，peeled commit 是否为 `961a3d0cc169e82b261d83207b0ec802007e292b`
- [ ] **22.84. v1.0.6 Release 是否真实** — `NSFWTrack v1.0.6` 是否为非 Draft、非 Pre-release 的正式 Release，地址是否为 `https://github.com/choneer/nsfwtrack/releases/tag/v1.0.6`
- [ ] **22.85. B3-B5 发布后开发边界** — 是否保持应用版本 1.0.6，只写 Unreleased，未移动 v1.0.6 或更早 tag / Release

## 备份恢复检查

- [ ] **23. JSON 导出是否完整** — 是否包含 items、tags、creators、collections、item_tags、item_creators、item_collections、user_item_states、saved_views、item_activity、app_settings
- [ ] **24. CSV 导出是否可读** — 是否包含条目、标签、创作者、合集、状态等本地字段
- [ ] **25. 恢复格式校验** — 非本项目导出的 JSON 是否拒绝
- [ ] **26. 恢复事务性** — 非法 JSON 或恢复失败是否不会破坏现有数据库
- [ ] **27. 本地边界** — 是否没有外部 URL 请求、自动同步或远程图片拉取
- [ ] **28. 备份预览** — 预览接口是否要求登录、只校验不写库、返回核心表、合集表、saved_views、item_activity、关联数量和 schema
- [ ] **29. 上传大小限制** — 是否只限制备份恢复/预览上传，超限文件是否不会进入恢复逻辑
- [ ] **30. 错误提示** — 缺文件、非 JSON、超限、非法 JSON、schema 不匹配、缺字段和恢复异常是否有中英文友好提示
- [ ] **31. 备份校验 / dry-run 报告** — 是否在正式恢复前提供 error / warning / info 报告、旧备份兼容提示、只读说明和写入前备份建议

## 常见违规

```
❌ 引入外部爬虫代码（Phase 1 不做）
❌ 引入 requests/httpx 对外 HTTP 请求
❌ 写入第三方 token/cookie 管理逻辑（自身登录 session 与语言偏好 session 除外）
❌ 引入推荐/ML 依赖
❌ 修改 database.py 加入不需要的表
❌ 引入 JS 前端框架（用 HTMX 不是 React/Vue）
❌ 提交了 .env 文件（应在 gitignore）
❌ 密码硬编码在代码中（应从环境变量读取）
```

## 验收通过标准

全部 [✅] → 通知你确认
检查项 0 不通过 → 直接退回
其他 ≥1 个 [❌] → 退回开发修复
